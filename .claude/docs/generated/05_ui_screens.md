# 05. 화면/UI 명세

> 원본: `.claude/docs/user/화면구성.md`
> 반영: `01_tech_stack.md`(화면 스타일링), `03_state_event_logic.md`(FAULT 수동 복구, 통신 두절), `04_database_schema.md`(테이블 구조)
> 작성: coreagent · 상태: 확정

## 1. 페이지 목록

| # | 페이지 | 접근 조건 |
|---|---|---|
| 1 | 로그인 | 비로그인 상태 진입 |
| 2 | 메인 대시보드 | 로그인 필요 |
| 3 | 모터 그래프 | 로그인 필요, 상단 헤더 내비에서 진입 (2026-08-06 추가) |
| 4 | 모터 현황 | 로그인 필요, 상단 헤더 내비에서 진입 (2026-08-06 추가) |
| 5 | 모터 상세 | 로그인 필요, 카드 클릭 시 진입 |

> 2026-08-06 재정리안(`.claude/docs/user/모터 AI - 메인 대시보드 (재정리).pdf`) 반영: 메인 대시보드는 유지하고, **모터 그래프(§3-A)** 와 **모터 현황(§3-B)** 두 페이지를 추가한다. 상세는 카드 클릭으로만 진입하는 하위 화면이라 헤더 내비에 넣지 않는다.

## 2. 로그인 페이지

- 입력: 이메일(ID) + 비밀번호 (`company_contacts.email`, `password_hash` 대조)
- 부가 검증: 접속 IP가 `company_contacts.allowed_ip`에 값이 있는 경우 해당 IP만 허용 (MAC 주소 체크는 미적용 — §5-1 참고)
- 로그인 시도는 성공/실패 관계없이 `login_logs`에 기록 (성공 시 `contact_id` 채움, 실패 시 NULL 허용)
- 성공 시 메인 대시보드로 이동

**화면 구성 확정 (2026-08-05)** — 기존에는 동작 규칙만 정의돼 있어 구현이 제목과 입력칸 2개로 끝나 있었다. 이 화면은 **고객이 서비스를 처음, 그리고 로그인 전에는 유일하게 마주하는 화면**이므로 무엇을 해주는 서비스인지가 드러나야 한다.

위에서부터 네 부분으로 구성한다.

1. **서비스 소개** — 아이콘 + 서비스명 + 한 줄 설명. 값은 `config.SERVICE_NAME` / `SERVICE_ICON` / `SERVICE_TAGLINE`.
2. **로그인 폼** — 이메일·비밀번호·로그인 버튼.
3. **시연용 계정 안내** — `st.expander`로 접어 둔다. 펼치면 계정 목록과 공통 비밀번호가 나온다. 계정 목록은 **DB에서 조회**한다(`list_demo_accounts()`) — 화면에 하드코딩하면 시드 구성이 바뀔 때 안내와 실제가 어긋난다.
4. **핵심 기능 3가지** — 실시간 감시 / AI 진단 / 리포트 자동 생성. 값은 `config.SERVICE_HIGHLIGHTS`.

- **폭 제한**: `main.py`가 대시보드 기준으로 `layout="wide"`를 쓰기 때문에, 그대로 두면 폼이 화면 전체 폭으로 늘어져 더 휑해 보인다. `st.columns([1, 2, 1])`의 가운데 열에 배치한다.
- 시연 관점: 계정 안내가 화면에 없으면 시연 자리에서 README를 찾아봐야 로그인할 수 있다. 접어 두는 것은 평소 화면을 깔끔하게 유지하기 위한 절충이다.

**계정 자동 입력 (2026-08-05 추가)** — 시연·개발 중 매번 계정을 타이핑하지 않도록 폼을 미리 채운다.

- **첫 진입 시에만** 첫 데모 계정과 공통 비밀번호를 채운다. 사용자가 지운 값은 다시 되살리지 않는다 — 다른 계정을 직접 입력하려 할 때 방해가 되기 때문이다.
- 시연용 계정 목록의 각 행에 **[채우기] 버튼**을 둔다. demo1 ↔ demo2를 오갈 때 다시 타이핑하지 않아도 된다.
- `config.PREFILL_DEMO_CREDENTIALS`로 끈다. **실제 서비스 배포 시 `False`로 바꾼다.**
- **구현 주의**: [채우기] 버튼은 폼 아래에 있어 클릭 시점에 위젯 key의 `session_state`를 바꿀 수 없다(`StreamlitAPIException: cannot be modified after the widget ... is instantiated`). 버튼은 요청만 별도 세션 키에 남기고, **스크립트 최상단(위젯 생성 전)** 에서 값을 채운다.

## 2-1. 공통 상단 헤더 (2026-08-05 확정)

로그인 후 모든 페이지 최상단에 둔다. **사이드바는 사용하지 않는다.**

```
⚙️ 모터 AI 모니터링                          (주)한국모터스
                                                  김철수   [로그아웃]
[메인 대시보드][모터 그래프][모터 현황][관리자]   NORMAL 정상  WARNING 경고  DANGER 위험  FAULT 고장
───────────────────────────────────────────────────────────
```

- **좌측**: 서비스 아이콘 + 서비스명. 로그인 화면(§2)에서 세운 브랜드가 로그인 후에도 이어진다.
- **우측**: `회사명`(굵게) + `담당자명`(작게), 세로 구분선, 테마 안내(§5-5), 로그아웃 버튼. 담당자가 지금 어느 회사 계정으로 보고 있는지 늘 확인할 수 있다.
- 회사명은 로그인 시 `company_contacts ⨝ companies` 조회 결과를 세션(`company_name`)에 담아 재사용한다. 매 화면 표시되는 값이라 페이지마다 다시 조회하지 않는다.

**컬럼 구성**: `st.columns([4.6, 4.2, 1.2])` — 브랜드 / 로그인 정보+테마 / 로그아웃.

**로그인 정보와 테마 안내는 한 `st.markdown` 블록에 함께 담는다.** 별도 컬럼으로 나누면 폭이 좁아질 때 한쪽이 밀려 다른 쪽 자리를 차지한다(실제로 4컬럼 구성에서 회사명이 사라지고 그 자리에 테마 안내가 표시되는 문제가 발생했다). 같은 flex 컨테이너에 두면 폭이 부족할 때 회사명이 말줄임으로 줄고 테마 안내는 `flex: 0 0 auto`로 고정된다.

**사이드바를 쓰지 않는 이유**: 일반 웹 서비스의 상단 헤더 형태가 담당자에게 익숙하고, 좁은 사이드바에 담당자명과 로그아웃만 두면 공간 낭비가 크다. 페이지 이동은 `st.navigation(position="hidden")`으로 목록을 감추고, 아래 방식으로 처리한다.

- **메인 페이지 간 이동(메인 대시보드 / 모터 그래프 / 모터 현황)**: 헤더 하단에 페이지 이동 내비 버튼 3개를 둔다(2026-08-06 추가). 현재 페이지는 primary 버튼으로 강조한다. 목록은 `navigation.HEADER_NAV_PAGES`, 렌더는 `components._page_nav()`. `page_header(active=...)`로 활성 페이지를 넘긴다.
- **내비는 모든 로그인 화면에 그린다 (2026-08-10 변경).** 종전에는 `active=None`이면 내비 자체를 그리지 않아 모터 상세에 `← 대시보드` 하나뿐이었고, 그래프로 가려면 두 번 눌러야 했다. 이제 `None`은 "강조할 페이지 없음"만 뜻한다(§4.1).
- **상세로 진입**: 모터 카드/현황 카드의 클릭(카드 전체가 클릭 영역), 복귀는 상단 내비의 [메인 대시보드].

**상태 범례를 내비 줄 오른쪽에 상시 노출한다 (2026-08-12 사용자 요청).** `NORMAL 정상 · WARNING 경고 · DANGER 위험 · FAULT 고장` 네 칩(`components._status_legend()`, `.status-legend`).

- **왜**: 모든 화면이 상태를 영문 배지로만 표시해, 처음 보는 사람은 배지 뜻을 화면 어디에서도 알 수 없었다 — 임계값 표(§4.2)까지 들어가야 한글 이름이 나온다. 이 서비스의 가장 중요한 기준 정보라 어느 페이지에서도 보여야 한다.
- **왜 여기**: 내비 컬럼 구성 `[1.3]×N + 나머지`의 **마지막 빈 열이 이미 있었다**(실측 빈 폭 1600px 화면 595px · 1440px 528px · 1280px 460px · 1100px 384px). 세로 공간을 더 쓰지 않고, `page_header()`를 모든 페이지가 부르므로 한 곳만 고치면 전 화면에 노출된다. 실측: 대시보드·그래프·현황·관리자·모터 상세 5개 화면 모두 노출, 1100px에서도 한 줄 유지(줄바꿈은 허용해 뒀다).
- **한계**: 문서 흐름 안이라 **스크롤하면 사라진다.** 스크롤 중에도 필요하면 갱신 카운터처럼 `position: fixed`로 띄우는 방법이 있다(`REFRESH_COUNTDOWN_*`, 좌하단이 비어 있다).
- **WARNING 한글 표기를 `경고`로 통일했다 (2026-08-12).** 종전에는 같은 상태를 `주의`(config `STATUS_KOREAN_LABELS`, 임계값 표 헤더, 대시보드 "주의 관찰" 타일)와 `경고`(차트 임계선 범례, 상세 캡션, 관리자 임계값 화면)로 나눠 불렀다. 범례를 상시 노출하면 이 불일치가 그대로 드러나므로 `경고`로 맞췄다 — 바뀐 곳: config, 대시보드 타일("경고 관찰"), 리포트 템플릿 임계값 표 헤더.

§3.1 컨텍스트 줄에서는 회사명을 뺀다 — 헤더와 중복되기 때문이다.

## 3. 메인 대시보드

### 3.1 상단 — 회사/현황 정보

| 표시 항목 | 데이터 출처 |
|---|---|
| 회사 정보 | `companies` (로그인한 담당자의 `company_id` 기준) |
| 등록된 모터 수 | `motors` count (해당 company_id) |
| 서비스 시작 일자 | `companies.created_at` |
| 총 운영 일수 | `오늘 - companies.created_at` (일 단위, 보강: 계산식 명시) |
| 상태별 모터 수 (NORMAL 제외) | 각 모터의 **대표 상태**(`03_state_event_logic.md` §2 — 4개 지표 중 최고 심각도) 기준 WARNING/DANGER/FAULT 개수 집계 |

**배치 확정 (2026-08-05)** — 위 5개 항목은 모두 표시하되, **매일 확인할 값과 아닌 값을 분리**한다.

담당자가 아침에 대시보드를 여는 이유는 "지금 문제 있나, 있으면 뭘 해야 하나"이지 계약 정보 확인이 아니다. 회사명·모터 수·서비스 시작일·운영 일수를 `st.metric` 타일 4칸에 쓰면, 정작 답이 필요한 질문이 화면에서 사라진다.

- **컨텍스트 줄 (캡션 1줄)**: `(주)한국모터스 · 모터 3대 · 서비스 시작 2025-06-19 (412일째)` — 회사 정보, 등록된 모터 수, 서비스 시작 일자, 총 운영 일수를 여기에 모은다.
- **요약 타일 5칸 — 상태 하나에 타일 하나 (2026-08-12 사용자 요청, 종전 4칸 구성을 대체)**

| 타일 | 값 | 보조줄 | 답하는 질문 |
|---|---|---|---|
| 바로 조치 필요 | `FAULT` **n대** | `고장 · 24h +n` | 지금 멈춰 있는 설비가 있나 |
| 조치 필요 | `DANGER` **n대** | `위험 · 24h +n` | 지금 손대야 할 설비가 있나 |
| 관찰 필요 | `WARNING` **n대** | `경고 · 24h +n` | 지켜봐야 할 설비가 있나 |
| 이상 없음 | `NORMAL` **n대** | `정상` | 나머지는 몇 대인가 |
| 마지막 수집 | 최신 텔레메트리 시각(**상대**) | `YYYY-MM-DD HH:MM:SS · 정상 수집 중` | 데이터는 들어오고 있나 |

- **값 앞에 영문 상태명을 붙인다** (`summary_tiles()`의 선택 키 `status_key`, 2026-08-12 사용자 요청). 화면 전체가 상태를 영문으로 표시하므로(카드 배지·차트 배지·이벤트 목록·헤더 범례), 요약 타일에도 같은 말이 있어야 "바로 조치 필요 4대"와 "FAULT 배지가 붙은 카드 4장"이 같은 것임이 이어진다. 숫자(30px)보다 작은 14px에 **타일 액센트와 같은 색**(`var(--tone)`)이라 값의 무게는 그대로 두면서 색으로도 연결된다.

- **왜 상태별로 쪼갰나.** 종전 `조치 필요`(DANGER+FAULT 합산)와 `최근 24시간 이벤트`(악화 n · 회복 n)는 **집계 규칙을 알아야 읽히는 숫자**였다 — 왜 17인지, 무엇이 '악화'인지가 화면 어디에도 없었다. 상태 한 칸에 타일 하나면 규칙이 없어지고, 헤더 상태 범례(§2-1)와 같은 어휘·같은 색으로 1:1 대응된다.
- **FAULT와 DANGER를 나눈 이유**: 대응이 다르다. FAULT는 정비 완료 확인이 필요한 정지 상태(바로 아래 배너·버튼이 그 흐름을 처리한다), DANGER는 가동 중 악화다. 합치면 그 차이가 숫자 하나에 묻힌다.
- **보조줄의 `24h +n`이 "밤사이 무슨 일이 있었나"를 대신한다.** 상태 4칸은 전부 '지금 이 순간' 스냅샷이라, 그대로 두면 아침에 출근한 담당자가 지난밤 변화를 볼 수단이 사라진다(종전 이벤트 타일이 그 질문을 맡고 있었다). `services.company.build_summary()`의 `recent_worsened_into` — 최근 `DASHBOARD_RECENT_WINDOW_HOURS`(24시간) 안에 **그 상태로 악화 전이한 건수**다. 상태별로 쪼갠 만큼 종전 `악화 71`보다 정확하다. 0이면 보조줄에서 뺀다(없는 변화를 `+0`으로 적지 않는다).
  - **회복 전이는 세지 않는다.** 예컨대 `FAULT → DANGER`로 내려온 것은 DANGER 칸에 유입이지만 나빠진 일이 아니다. 함께 세면 `+n`의 뜻이 흐려진다.
  - 종전 `회복 n`은 **집계가 틀려 있었다** — `전체 − 악화`라 FLAT(같은 상태 기록, `previous_status`가 NULL인 첫 기록, connectivity 이벤트처럼 상태값이 규칙 밖인 행)까지 회복으로 셌다. 타일과 함께 사라졌다.
- **마지막 수집은 상대 시각을 크게, 절대 시각을 보조줄에 둔다.** 절대 시각만 두면 "몇 분 전이지?"를 암산해야 신선도를 알 수 있다. 보조줄의 절대 시각은 **연도까지 적는다**(`SUMMARY_COLLECTED_FORMAT`, 사용자 요청) — 데이터가 48시간 창 안이라 연도는 늘 같지만 시각을 그대로 옮겨 적을 수 있어야 한다. 1280px 화면 타일 폭 214px에서도 한 줄에 들어간다(실측).
- **내역은 값 아래 보조줄로 표기한다.** `st.metric`의 `delta`는 값이 증감이 아니어도 항상 상승 화살표(`↑`)를 붙인다. `delta_color="off"`는 색만 지울 뿐 화살표는 남아, `↑ 정상 7대`·`↑ 정상 수집 중`처럼 늘어난 것으로 오해되는 표시가 나온다(2026-08-05 확인).

**타일 자체 렌더링으로 전환 (2026-08-07 변경).** `st.metric`을 버리고 `components.summary_tiles()`가 그리는 카드로 바꿨다. 화면 첫 화면인데 라벨·값·보조줄이 아무 테두리 없이 텍스트로만 놓여 무게가 실리지 않았고, `.metric-sub`를 음수 마진으로 끌어올려 붙이는 방식이라 값과 보조줄 사이 간격도 불안정했다.

- **모터 카드와 같은 언어**로 그린다 — 상단 액센트 3px + 카드 배경 + 테두리(`.summary-tile`). 대시보드 전체가 한 벌로 보인다.
- **액센트 색이 곧 상태다.** 장식이 아니라 정보이므로 숫자를 읽기 전에 색으로 먼저 안다.

| 타일 | 색 규칙 |
|---|---|
| 바로 조치 필요 / 조치 필요 / 관찰 필요 | 1대 이상이면 그 상태색(FAULT 빨강 · DANGER 주황 · WARNING 앰버), **0대면 NORMAL색** |
| 이상 없음 | NORMAL색 고정 |
| 마지막 수집 | 수집 중이면 NORMAL색, 데이터 없으면 DANGER색 |

- **0대면 초록**인 이유: 고장 0대가 빨간 타일로 보이면 숫자를 읽기 전에는 문제가 있는 것처럼 보인다. 색이 먼저 읽히므로 색과 뜻이 어긋나면 안 된다.

- **조치 필요 타일은 고장이 있으면 FAULT색(빨강), 위험만 있으면 DANGER색(주황)이다.** 2026-08-07 팔레트 재정리(§5-3) 전에는 FAULT가 슬레이트라 옆 WARNING보다 흐려 보여 이 타일에서 FAULT 톤을 일부러 뺐었다. 램프가 단조 증가로 바뀌어 그 우회를 걷어냈다.
- 값 글꼴 30px / 라벨 12px / 보조줄 11.5px로 위계를 분명히 한다.
- **열 수는 5열 → (1180px 이하) 3열 → (820px 이하) 2열로 명시한다.** `repeat(auto-fit, minmax(...))`를 쓰면 폭에 따라 열 수가 제멋대로 바뀌어 타일이 홀로 남는 줄이 생긴다. 실측 타일 폭: 1600px 화면 278px · 1280px 214px(둘 다 1줄) · 1100px 305px(2줄).
- 최근 이벤트 창은 `config.DASHBOARD_RECENT_WINDOW_HOURS = 24`.
- "마지막 수집"은 통신 두절 감지(`03_state_event_logic.md` §4.4)가 구현되기 전까지 **데이터 유입 여부를 확인할 유일한 수단**이다.

**조치 배너 (2026-08-05 추가)** — 요약 타일 바로 아래, 모터 카드 위에 둔다. 타일은 "몇 대"만 알려주므로 담당자가 어느 설비인지 찾으려면 카드를 훑어야 한다. 배너는 **설비 이름과 해야 할 일**을 직접 말한다.

| 조건 | 상태색 | 문구 |
|---|---|---|
| 미확인 FAULT 지표 있음 | FAULT | 고장 상태입니다. 정비 후 완료 확인을 해주세요 |
| 대표 상태 FAULT인데 미확인 지표는 없음 | FAULT | 정비 완료 확인을 마쳤지만 여전히 고장 임계를 넘는 값이 들어오고 있습니다 |
| 대표 상태 DANGER | DANGER | 위험 상태입니다. 진단 리포트를 확인해주세요 |

두 번째 행이 필요한 이유: 정비 완료를 확인해도 계측값이 즉시 임계 아래로 내려가지는 않는다. 이때 대표 상태는 FAULT지만 확인 버튼은 사라지므로, 첫 번째 문구를 그대로 두면 "완료 확인을 하라는데 버튼이 없는" 상태가 된다. 담당자가 할 일도 다르다 — 확인이 아니라 **현장 재점검**이다.

**색상 확정 (2026-08-05)** — `st.error`/`st.warning`을 쓰지 않고 §5-3 상태색을 그대로 쓰는 자체 배너(`alert_banner()`)로 그린다.

- `st.error`(빨강)/`st.warning`(노랑) 두 가지뿐이라 DANGER 배너가 노랑으로 나왔다. **카드에서는 DANGER가 빨강인데 배너만 노랑이면 담당자가 심각도를 잘못 읽는다.**
- **배너는 상태에 관계없이 같은 형태(좌측 테두리 + 옅은 배경)를 쓴다.** 종전에는 FAULT만 배경을 채웠는데, 상태색이 슬레이트라 테두리만으로는 아래 DANGER 배너보다 약해 보였기 때문이다. 2026-08-07 팔레트 재정리(§5-3)로 FAULT가 램프 최상단(빨강)이 되어 채움 우회를 걷어냈다 — 심각도는 색이 말한다.
- **정비 완료 확인 안내 (2026-08-06 재설계)**: 미확인 FAULT 목록은 상태색 단색 배너 대신 **🔧 아이콘 + 밝은 강조 박스**("정비 완료 확인 필요 — N대가 고장(FAULT)…")로 부각한다(짙은 단색은 '건조'해 조치 요구로 읽히지 않는다). 그 아래 **모터별 카드**(이름·모델명·설치 위치)를 나열하고 각 카드 **안에 작은 [정비 완료 확인] 버튼**을 둔다 — 어느 설비의 버튼인지 카드가 바로 위에서 알려준다. `render_maintenance_confirm()`, 확인은 §4.3 다이얼로그로 이어진다.
- 대표 상태 집계는 §3.2 카드용으로 이미 조회한 모터 목록을 재사용한다. 대표 상태 판정에 지표별 최신 로그 조회가 필요해 따로 계산하면 중복 비용이 든다.
- **시드 데이터가 `companies.created_at`·`motors.created_at`을 명시적으로 채운다.** DB 기본값(현재 시각)에 맡기면 "총 운영 일수"가 항상 0일이 되고 §4.1 등록일자도 전부 오늘이 되어, 두 항목이 시연에서 아무것도 보여주지 못한다. 회사별로 다른 값을 주며 모터 등록일은 소속 회사의 서비스 시작일보다 뒤가 되도록 한다.

### 3.2 모터별 카드

```
[모터 이미지] --> [API 이미지] --> [AI Agent 아이콘]
   모터 모델명
   [상태 배지: WARNING]
   26/07/01 13:05
```

- 모터 이미지 → API → AI Agent 흐름을 "데이터가 흐르는 것처럼" 애니메이션 표시
  - 구현: `01_tech_stack.md` §2.5에서 확정한 커스텀 CSS 애니메이션 또는 `streamlit-lottie` 사용
- 상태 배지: 모터 대표 상태 + 최근 상태 변경 일시(`motor_status_logs` 최신 `created_at`)
- 상태별 색상 (보강 — report_template.html과 통일된 팔레트 재사용, 아래 §4 참고)
- 카드 클릭 시 모터 상세 페이지로 이동

**구현 확정 (2026-08-05)**

- **카드 전체가 클릭 영역이다 (2026-08-05 확정)**. Streamlit은 임의 HTML 요소에 콜백을 붙일 수 없으므로, 카드와 같은 컨테이너에 버튼을 두고 **CSS로 카드 위를 투명하게 덮는다**. 위젯 `key`가 DOM에 `st-key-{key}` 클래스로 노출되는 것을 이용해 `[class*="st-key-motorclick-"] button { opacity: 0 }`으로 잡는다. 클릭 시 `selected_motor_id`를 세션에 담고 `st.switch_page`로 이동한다.
  - **링크(`<a href="?motor=...">`) 방식은 쓸 수 없다.** `target="_self"`를 붙여도 브라우저가 페이지를 새로 로드하고, 그러면 Streamlit 세션이 새로 만들어져 `session_state`가 비워진다 — 로그인 상태가 풀려 로그인 화면으로 돌아간다. 실제로 이 방식을 먼저 시도했다가 확인한 문제다.
  - 버튼 라벨은 `"{모터명} 상세 보기"`로 둔다. 화면에는 보이지 않지만 스크린리더에는 읽혀 접근성이 유지된다.
  - 클릭 가능함을 알리기 위해 hover 시 카드가 살짝 떠오르고 테두리가 상태색으로 바뀐다. `prefers-reduced-motion` 환경에서는 이동 효과를 끈다. (`상세 보기 ›` 안내는 2026-08-10에 카드 높이를 줄이며 제거했다 — hover 효과가 그 역할을 대신한다.)
- **카드 안에는 위젯을 두지 않는다.** 카드 전체가 클릭 영역이라 내부 버튼과 클릭이 겹치고, 카드마다 버튼 유무가 달라지면 높이도 어긋난다. FAULT 모터의 정비 완료 확인은 §3.1의 **정비 완료 확인 안내(모터별 카드)** 에서 처리한다.

**그리드 (2026-08-05 확정)**

- 한 행에 `config.MOTOR_CARD_COLUMNS = 5`개. COMP-002(10대)는 2행이지만 **COMP-001은 200대라 카드 그리드가 40행으로 매우 길고 무겁다** — 전체 조망은 모터 현황(§3-B) 페이지가 맡는다.

**표시 개수 제한 (2026-08-07 임시, 개수 추후 확정)** — `config.DASHBOARD_MOTOR_CARD_LIMIT`(현재 20)개만 그린다. `None`이면 제한 없이 전부 그린다.

- **심각도 순으로 고른다** (`services.motors.select_priority_cards()`). `list_company_motors()`는 등록 순(motor_id)을 유지하지만 앞에서부터 그냥 자르면 안 된다 — COMP-001은 200대 중 조치 필요가 17대이고 motor_id 순으로는 앞쪽에 거의 들어오지 않아, **문제를 발견하라고 있는 화면이 문제를 전부 가리게 된다.**
- 같은 상태끼리는 등록 순을 유지한다(안정 정렬). "설비가 늘 같은 자리에 있어야 담당자가 위치로 기억한다"는 원래 의도를 그 안에서 지킨다.
- 카드 위에 캡션으로 `조치가 급한 순으로 N대만 표시합니다 (전체 M대 · 목록 전체는 '모터 현황' 페이지에서…)`를 둔다. 일부만 보고 있다는 사실을 화면이 직접 말해야 한다.
- **상단 요약(§3.1)과 조치 배너는 제한 이전의 전체 목록으로 계산한다.** 집계와 배너의 설비 이름은 200대 기준 그대로다.
- **성능 개선은 아니다.** `list_company_motors()`는 여전히 200대 전부에 대해 모터당 쿼리를 돈다. 줄어든 것은 렌더링(무거운 SVG 카드)뿐이며, 쿼리까지 줄이는 것은 `remaining_work.md #7`에 남아 있다.
- 행 사이 간격은 `config.MOTOR_CARD_ROW_GAP_PX = 16`. **카드가 아니라 컬럼 블록의 `margin-bottom`에 준다.** 클릭 버튼이 `inset: 0`으로 블록을 덮으므로 카드에 margin을 주면 간격까지 클릭 영역이 된다. margin은 요소 박스 밖이라 블록 크기가 변하지 않아 카드 사이 빈 공간을 눌러도 반응하지 않는다.

**세로 길이 고정 (2026-08-05 확정)** — 카드마다 높이가 달라 그리드가 들쭉날쭉해지는 문제를 다음으로 해결한다.

원인은 두 가지였다. ① 이상 지표는 값 글꼴이 14px → **18px**로 커져 그 행만큼 아래가 밀린다. ② 5열이라 카드가 좁아 모터명·설치 위치가 **모터마다 다른 줄 수로 접힌다**.

카드 전체에 `height`를 주면 내용이 잘리므로 **구성 요소별로 높이를 고정**한다.

| 요소 | 높이 | 넘침 처리 |
|---|---|---|
| 모터명 + 배지 | 22px | 말줄임 |
| 모델명 · 설치 위치 | 30px | 최대 2줄 clamp |
| 지표 행 (이름 · 값 · 설명) | 21px | 말줄임 |
| 게이지 | 7px | — |
| 추이 영역 | 스파크라인 높이 + 10px | — |

- 이상 지표는 **글꼴만 키우고 `line-height`는 고정 행 높이(21px)를 그대로 쓴다.** 그래야 강조하면서도 아래를 밀지 않는다.
- **설명은 지표 행 오른쪽 끝에 붙인다 (2026-08-10 변경).** `온도 22.1°C ⋯ 고장 임계의 25%`처럼 이름·값과 같은 줄에 둔다. 종전에는 게이지 아래 별도 줄(14px + 여백 3px)이라 지표마다 17px, **카드당 68px**을 썼다. 폭이 좁아지면 이름·값이 아니라 설명이 먼저 줄어든다(`flex: 0 1 auto` + 말줄임).
- **설명은 모든 지표에 둔다.** 이상일 때만 넣으면 카드마다 줄 구성이 달라진다. 정상 지표는 `고장 임계의 N%`로 채우며, 덤으로 여유가 얼마인지 읽힌다.
**카드 하단 추이 (스파크라인)**

- 카드마다 **가장 심각한 지표 하나**의 추이를 그린다(`readings[0]`). 132×28px 인라인 SVG `polyline`이며 축·임계선이 없다 — 값을 읽는 도구가 아니라 방향을 말하는 그림이다. 오른쪽에 `3시간 ↑ 상승` 같은 요약을 붙인다.
- **데이터는 최근 `TREND_WINDOW_HOURS`(3시간) 원본이다 (2026-08-12 사용자 확정).** 종전에는 6시간을 15분 버킷 평균(24구간)으로 압축했다. 두 그래프 화면이 원본 표시로 간 뒤 카드만 평균으로 남아 같은 지표를 화면마다 다른 밀도로 그리고 있었고, **"데이터를 그대로 보여주는 쪽이 사실적인 신뢰감을 준다"는 판단**으로 맞췄다. 창도 모터 그래프 페이지와 같은 3시간이다.
- **비용 실측(카드 20장)**: 추이 조회 **7ms**, 카드당 점 36~1,029개(수집 주기 10~60초 차이), 점 합계 6,693, SVG 마크업 69KB(카드당 평균 3KB), 로그인 → 스파크라인 렌더 **1,351ms**.
- **트레이드오프를 알고 택했다.** 132×28px에 1,029점이면 픽셀당 8점이라 선이 아니라 **떨림이 있는 띠**로 보인다. 반대로 60초 주기 모터는 점이 36개라 매끄러운 선이다 — 같은 화면에서 카드마다 그림의 성격이 다르다. 되돌리려면 `attach_card_trends()`의 호출을 `get_metric_trend()`로 바꾸면 된다(평균 함수와 `TREND_BUCKETS`를 남겨 뒀다).
- **방향 판정은 노이즈에 흔들리지 않는다.** `_trend_direction()`이 앞 1/3 평균과 뒤 1/3 평균을 비교해 `TREND_CHANGE_THRESHOLD`(3%)를 넘을 때만 상승/하락으로 적는다. 마지막 값 하나가 튀어도 방향은 바뀌지 않는다.

- **하단 줄(상태 변경 시각 + `상세 보기 ›`)은 두지 않는다 (2026-08-10 변경).** 28px(16px + 여백 12px)을 카드마다 쓰는데, 상태 변경 시각은 바로 위 추이 줄과 이벤트 목록(§3.3)에도 있고 진입 안내는 hover 효과가 대신한다.
- **카드 높이 실측**: 위 두 변경으로 지표 설명 68px + 하단 줄 28px = **약 96px** 줄었다. 현재 높이 **346px**(카드 20장 전부 동일, 1400px 뷰포트 3열 기준).
- 이상 카드에 **그림자를 주지 않는다.** 크기는 그대로여도 시각적으로 카드가 커 보여 정상 카드와 달라 보인다. 구분은 상단 3px 테두리 색과 옅은 배경색만으로 한다.
- **흐름 애니메이션은 커스텀 CSS로 구현한다** (§2.5의 두 선택지 중 CSS 확정). `streamlit-lottie`는 애니메이션 JSON을 외부에서 받아와야 해 오프라인·CSP 제약이 있는 반면, CSS 키프레임은 추가 의존성과 네트워크 요청이 전혀 없다.
  - 구조: `⚙️ 모터 —— 🔌 API —— 🤖 AI Agent`. 두 연결선의 배경 그라데이션을 흘려보내 데이터 이동을 표현하고, 두 번째 선은 반 주기 늦게 출발시켜 흐름 방향이 드러나게 한다.
  - 색상은 모터 대표 상태를 따른다(`STATUS_COLORS`를 CSS 변수 `--flow-color`로 주입).
  - 주기는 `config.DATA_FLOW_ANIMATION_SECONDS`, 노드 구성은 `config.DATA_FLOW_NODES`로 중앙화한다.
  - `prefers-reduced-motion: reduce` 환경에서는 애니메이션을 정지시킨다.

### 3.3 이벤트 발생 내역 리스트

- 컬럼: 발생 일시, 모터명, 상태 변화, **값 변화**, 발생 사유, 리포트 버튼. 폭은 `config.EVENT_COLUMN_WIDTHS_WITH_MOTOR`.
- 데이터 출처: `motor_status_logs` (모터 대표 상태 기준 전이 이벤트, 최신순)
- 최근 **최대 10개** 표시

**결과 상태 필터 (2026-08-07 추가)** — `config.DASHBOARD_EVENT_STATUSES`(현재 `FAULT`, `DANGER`)에 해당하는 전이만 싣는다. `None`이면 전체를 보여준다.

- 대시보드는 "지금 손대야 할 것"에 답하는 화면이다. `NORMAL → WARNING` 같은 **관찰 단계 전이가 섞이면 조치가 필요한 건이 10칸 안에서 밀려난다.**
- **필터는 화면이 아니라 조회에서 건다.** `LIMIT`이 필터보다 먼저 걸리면 최근 10건이 전부 관찰 단계일 때 조치 대상이 한 건도 안 보인다.
- **필터가 걸렸다는 사실을 캡션이 직접 말한다** — `고장 · 위험 전이 N건 · 모터별 이력은 상세 페이지에서 볼 수 있습니다.` 안 그러면 WARNING 전이가 없는 것을 데이터 누락으로 오해한다. 빈 목록일 때도 `최근 고장 · 위험 이벤트가 없습니다`로 범위를 밝힌다.
  - 2026-08-10 이전에는 `주의(WARNING)를 포함한 전체 이력은 모터별 상세 페이지에서…`라고 안내했다. **§4.4가 같은 필터를 쓰게 되면서 이 약속이 사실이 아니게 되어** 문구를 고쳤다.
- 이 필터 덕에 **모든 행에 리포트 버튼이 붙는다** — 버튼 노출 조건(`DANGER`/`FAULT`)과 같은 범위이기 때문이다. §4.4도 같은 필터라 마찬가지다.
- 상태 한글 표기는 `config.STATUS_KOREAN_LABELS`를 쓴다(알림 문구와 공유).

**값 변화 컬럼 (2026-08-07 추가)** — `94.73 → 95.12 dB` 형태로 계측값의 변화를 함께 보여준다.

상태 전이는 "어느 상태에서 어디로"만 말하고 **"얼마였다가 얼마가 됐는지"는 말하지 않는다.** 담당자가 급한 정도를 가늠하려면 수치가 필요하다 — 임계를 아슬하게 넘었는지, 크게 뛰었는지에 따라 대응이 다르다.

- **데이터**: 이벤트 시각의 `motor_telemetry` 행에서 해당 지표 값(현재), 그 직전 수집 행에서 같은 지표 값(이전). `metric_name`이 행마다 다르므로 SQL `CASE`로 컬럼을 고르고, 이전 값은 상관 서브쿼리로 가져온다(`services/events.py`). `(motor_id, time DESC)` 인덱스를 탄다.
- **소수점 자릿수**: 기본 **2자리**(`config.EVENT_VALUE_DECIMALS`). **두 값이 같게 찍히면 구분될 때까지 최대 3자리 더 늘린다** — 임계 바로 앞뒤에서 전이가 일어나면 반올림에 묻혀 임계를 넘은 사실이 사라진다(실측: 진동 `5.97 → 6.03`이 1자리에서는 `6.0 → 6.0`).
- **표기**: 이 열의 값 글꼴을 **15px**로 리스트에서 가장 크게 둔다. 담당자가 급한 정도를 판단하는 수치이기 때문이다. 이전 값은 흐리게, 현재 값을 굵게(800) 해 시선이 "지금 얼마인가"에 먼저 간다. 화살표는 악화(DANGER색)·회복(NORMAL색)으로 물들인다.
- **값이 없을 때**: 첫 수집이라 이전 값이 없거나 계측 컬럼이 없는 지표(`connectivity`, `03 §4.4`)는 있는 쪽만 표시하고, 둘 다 없으면 `-`로 둔다. 빈칸으로 두면 데이터가 누락된 것처럼 보인다.

**행 구분선과 열 폭 (2026-08-07)** — 열이 6개로 늘면서 어느 값이 어느 행인지 눈으로 따라가기 어려워졌다.

- **구분선**: 헤더 아래 2px, 행마다 1px. `:has(.event-th)` / `:has(.event-when)`으로 **이벤트 행에만** 건다 — 범위를 좁히지 않으면 대시보드의 다른 컬럼 블록까지 선이 그어져 화면 전체가 표처럼 보인다.
- 선 색은 `border`를 쓴다. `border_soft`는 다크에서 surface와 거의 같아 선이 보이지 않는다.
**행 높이 보정 (필수)** — Streamlit이 블록 높이를 실제 콘텐츠보다 작게 보고해, 그대로 두면 **글자가 하단 구분선을 넘어간다.** 헤더와 데이터 행이 증상도 해법도 달라 **따로 처리해야 한다.**

| | 증상 | 해법 |
|---|---|---|
| 데이터 행 | 마크다운 래퍼를 한 줄(16px)로 보고 → 두 줄인 "발생 일시"(32px)가 넘쳐 구분선에 6px까지 달라붙음 | `[data-testid="stMarkdown"]`과 자식 div `height: auto !important` + 행 블록 `align-items: stretch` + 컬럼 `display:flex; align-items:center` |
| 헤더 | 블록을 라벨 글자보다 작게 보고 → 라벨이 구분선 아래로 삐져나옴 | 블록에 `align-items: flex-start; padding-top: 0.5em; min-height: 2.7em` |

- **어느 쪽도 `align-items: center`를 쓰면 안 된다.** Streamlit이 잘못 보고한 작은 상자를 기준으로 정렬하기 때문에, 데이터 행은 콘텐츠가 아래로 밀리고 헤더는 라벨이 구분선에 1px까지 달라붙는다. 헤더는 위에서 시작시키고(`flex-start`) 여백을 직접 준다.
- **헤더에는 `height: auto`가 듣지 않는다.** 오히려 `stMarkdown`이 3px로 무너진다. `padding-bottom`도 블록 높이에 반영되지 않아 `min-height`가 유일하게 통했다. 두 경로가 왜 다르게 동작하는지는 규명하지 못했다(Streamlit 내부 emotion 스타일 차이로 추정, **미확인**).
- **`min-height`·`padding`은 `em`으로 준다.** 라벨 글꼴만 키우고 이 값을 px 고정으로 두면 다시 넘친다(12px→16px로 올렸을 때 실제로 3px 넘쳤다).
- **컬럼명 글꼴은 본문과 같은 16px.** 12px일 때는 표의 머리글이 아니라 작은 주석처럼 읽혀 어느 열이 무엇인지 눈에 들어오지 않았다.
- 실측: 조치 전 헤더 글자 −5px(넘침) · 행 콘텐츠→선 6px → **조치 후 헤더 위 8 / 아래 10px, 행 10px, 넘침 0건**.
- 헤더는 `st.caption` 대신 `.event-th` 마크다운으로 그린다 — caption에는 CSS가 잡을 클래스가 없어 구분선을 걸 수 없다.
- **열 폭**은 실측 콘텐츠 필요량 기준으로 잡았다(1440 뷰포트, 전체 1270px): 발생일시 110 · 모터명 204 · 상태변화 161 · 값변화 107 · 발생사유 82. 종전에는 상태변화·발생사유에 253px씩 주고 있어 과할당이었다.

**리포트 버튼 규칙 (2026-08-04 확정)**

- **노출 조건**: `new_status`가 **DANGER 또는 FAULT**인 로그. (PDF를 요청 시 생성하는 방식이므로 최초에는 `report_pdf`가 항상 NULL이며, 이를 노출 조건으로 쓸 수 없다.)
- **클릭 시 동작 (2026-08-10 변경)**: **내용은 환경과 무관하게 항상 화면에 띄우고, 내려받기 형식만 환경에 따라 달라진다.**
  1. 저장된 `report_html`이 없으면 그 자리에서 만들어 저장한다 (진단 에이전트가 여기서 돈다)
  2. `report_pdf` 캐시가 있으면 그대로 쓰고, 없으면 PDF 변환을 시도해 성공 시 캐시한다
  3. 화면에는 **항상 HTML을 iframe으로 표시**한다
  4. 다운로드 버튼은 **하나만** 둔다 — PDF를 만들 수 있으면 `PDF 문서 다운로드`, 불가하면 `HTML 문서 다운로드`
- **왜 바꿨나.** 종전에는 PDF가 만들어지면 다운로드 버튼만 보여줬다. 담당자가 파일을 내려받아 열기 전에는 진단 내용을 확인할 수 없어, "이상 감지 → 즉시 원인 파악"이라는 흐름이 끊겼다. 반대로 PDF 환경과 HTML 환경에서 화면이 전혀 달라 보이는 문제도 있었다. 지금은 어느 환경이든 같은 화면을 보고, 차이는 버튼 문구 하나뿐이다.
- 다운로드 버튼을 두 개(PDF+HTML) 두지 않는 이유는 담당자가 무엇을 받아야 하는지 고민하게 만들기 때문이다. PDF가 되면 PDF가 정답이다.
- `get_report()`는 `{"html": str, "pdf": bytes | None}`을 반환한다. `pdf`가 `None`이면 그 환경에서 변환이 불가하다는 뜻이다.
- 어느 경로든 파일시스템에 저장하거나 조회하지 않는다 (메모리 내 처리).
- **표시 위치 (2026-08-05 확정)**: 결과는 `st.dialog` 모달로 띄운다. 리스트 행 안에서 바로 리포트를 렌더하면 표 레이아웃이 무너지기 때문이다. 클릭 결과는 세션 상태에 담아 rerun 후 모달에서 꺼내 쓴다.
- **렌더 API (2026-08-05 확정)**: HTML은 **`st.iframe`** 으로 인앱 표시한다. `st.iframe`은 HTML 문자열을 넘기면 `srcdoc`으로 렌더하므로 파일시스템을 경유하지 않는다. 기존에 명시했던 `st.components.v1.html`은 2026-06-01자로 제거가 예고된 API라 사용하지 않는다.
- **뷰어 높이 `REPORT_VIEWER_HEIGHT_PX` = 720 (2026-08-12, 종전 600).** 다이얼로그를 실제로 열어 측정하니 리포트 본문이 **4,053px**(A4 5페이지)여서 600px 창으로는 6.8화면을 스크롤해야 했다. **AI 진단은 5개 섹션 중 2번째**라 시연에서 반드시 내려야 하는 자리다. 모달이 뷰포트 전체를 쓰고 있어 여유가 있었고, 720 적용 후 5.7화면이 됐다. **720을 상한으로 둔 이유**: 세로 768px 노트북에서 모달 자체가 스크롤되기 시작한다.
  - 같은 측정에서 확인된 것: 가로 잘림 없음(본문 1175px < 창 1190px), 이중 스크롤 없음(다이얼로그는 고정이고 iframe 안에서만 스크롤).
- **`report_html`은 최초 열람 시 생성된다 (2026-08-07 변경, 위 §2.3 서술 정정).** 종전에는 진단 시점에 전건 생성했으나 콜드 스타트를 지배해 온디맨드로 옮겼다(`06_report_spec.md` §3). 저장된 HTML이 없으면 그 자리에서 만들어 저장하므로 **버튼이 보이는데 보여줄 것이 없는 상황은 여전히 발생하지 않는다** (`04_database_schema.md` §3.5).
- **최초 열람은 기다림이 있다 (2026-08-10).** 진단 에이전트 호출이 이 경로에 들어 있어 프로세스 첫 리포트가 실측 5~9초, 이후 4초대, 재열람은 캐시로 0.03초다. 무엇을 기다리는지 모르는 채 멈춰 있는 것이 대기 자체보다 나쁘므로 스피너 문구를 **"AI 진단 리포트를 생성하는 중입니다… (최초 열람은 몇 초 걸립니다)"** 로 둔다. 실측 표는 `02_architecture.md` §2.4.1.

## 3-A. 모터 그래프 페이지 (2026-08-06 추가 · 필터/임계선 보강)

> 재정리안 1페이지. 지표별로 모터 추이를 한눈에 훑으며, 어느 지표가 문제인지 바로 보는 화면.

- **표시 범위 필터(상단 4개 드롭다운)**:
  - **상태**(`GRAPH_STATUS_FILTER_ORDER`): 전체 / FAULT / DANGER / WARNING / NORMAL. 기본 전체.
  - **위치**: `installation_location` 값. 기본 전체.
  - **모델명**: `model_name` 값. 기본 전체.
  - **표시 최대 수**(`GRAPH_MAX_MOTORS_OPTIONS`): 10 / 20. 기본 `GRAPH_DEFAULT_MAX_MOTORS`(10). 4지표를 곱하면 차트가 많아지므로 한 화면 렌더량을 이 값으로 제한한다.
- **정렬**: 대표 상태 심각도 내림차순(FAULT → DANGER → WARNING → NORMAL), 동순위는 `motor_id`.
> **임계선은 회사 기본값 기준이다 (2026-08-11).** 이 배치는 열 헤더에 임계값을 한 번만 적고 모든 차트가 같은 Y범위를 공유해 **임계선 위치로 값을 읽는** 구조다. 모터마다 임계값이 다르면 그 전제가 깨지므로, 여러 대를 나열하는 이 화면은 기본값으로 그리고 **설정이 기본과 다른 모터가 섞여 있으면 캡션으로 알린다**(정확한 기준은 모터 상세에서 본다). 모터 상세(§4.1-A)는 한 대뿐이라 그 모터의 임계값을 그대로 쓴다.

- **레이아웃 — 모터 한 대가 가로 한 행 (2026-08-10 변경)**: 맨 위에 지표 4열(온도/진동/전류/소음) **헤더 라운드 박스**(지표명·단위 + 임계값 범례 `경고/위험/고장`)를 한 줄만 두고, 그 아래로 **모터마다 `st.container(border=True)` 한 개**가 4열 차트를 가로로 감싼다. 모터명·상태 배지는 그 행의 **왼쪽 위에 한 번만** 나온다. 헤더 색·라인 색은 지표별로 다르다(`THEMES[...]["metric_chart"]` — 상태색과 겹치지 않는 파랑/보라/청록/자홍). (상단에 별도 요약 문구는 두지 않는다.)
  - **종전 구조와 무엇이 달랐나.** 지표 열이 바깥 루프였다(열 하나에 모터들을 세로로 쌓음). 그래서 ① 모터명·상태 배지가 **열마다 4번 반복**됐고(모터 10대 = 이름 40개) ② 모터 사이 구분선을 열 경계 밖으로 그릴 수 없어 어디까지가 한 모터인지 눈으로 이어 붙여야 했다.
  - 모터를 행으로 묶으면 이름이 **모터당 1번**이 되고(실측 40 → 10), 테두리 컨테이너가 4열을 가로로 감싸 행 경계가 그대로 드러난다(실측 폭 1230px · 높이 187px로 모든 행 균일).
  - **상태 배지는 행 머리가 아니라 차트마다 붙인다 (2026-08-12 사용자 요청).** 행 머리의 배지 하나는 **대표 상태**(가장 심각한 지표)라 *어느* 지표가 그 상태인지 알려주지 못했다 — 온도·진동·전류가 정상인데 소음 때문에 FAULT인 모터도 행 전체가 FAULT로만 보였다. 이제 차트 4개 각각의 오른쪽 위에 그 지표의 상태 배지가 붙고(`.mg-cell-status`), 행 머리의 대표 배지는 뺐다(같은 정보 중복). 지표별 상태는 `list_company_motor_status()`의 `statuses`·`fault_metrics`에서 읽는다(미확인 FAULT는 수치가 내려가도 FAULT 유지, 03 §4.3).
  - **행 머리 왼쪽 위 = `(모터명) 상세 보기 ›` 버튼 하나 (2026-08-12 사용자 요청).** 종전에는 그래프에서 이상을 발견해도 상단 내비로 나가 대시보드에서 그 모터를 다시 찾아야 했다. 누르면 `selected_motor_id`를 세션에 담고 모터 상세로 `switch_page`한다 — 대시보드 카드와 같은 방식이다.
    - **모터명을 버튼 라벨 안에 넣는다.** 이름 마크다운과 버튼을 따로 두면(첫 구현) 둘 사이 간격을 좁혀도 두 덩어리로 읽히고, 버튼을 행 오른쪽 끝에 두면 이름과 1,200px쯤 떨어져 어느 모터의 버튼인지 눈으로 이어 붙여야 한다. 라벨 서식은 `GRAPH_DETAIL_BUTTON_LABEL`.
    - **모터명은 괄호로 묶는다.** `137호기 급수펌프 상세 보기`처럼 이어 붙이면 어디까지가 모터명인지 구분되지 않는다(사용자 지적).
    - 컬럼 비율 `[3, 9]`(1600px에서 약 360px), 버튼 높이는 CSS로 28px — 기본 38px이면 모터 행마다 10px씩 늘어난다. 라벨은 왼쪽 정렬한다: 버튼에 `justify-content`만 주면 듣지 않고(내부 래퍼 div가 버튼 폭을 다 차지한 뒤 그 안에서 다시 가운데 정렬한다 — 실측 344px 중 322px), 래퍼 `button > div`와 `button p`까지 함께 왼쪽으로 붙여야 행마다 글자 시작 위치가 맞는다.
- **차트 코드는 `app/ui/charts.py`가 갖는다 (2026-08-10).** 모터 상세(§4.1-A)가 같은 차트를 한 대에 대해 쓰므로, 축 구성·임계선·Y범위 규칙이 한 벌만 존재하도록 페이지 밖으로 꺼냈다. 상세는 `show_motor_row=False`로 행 머리를 생략하고, 높이·Y눈금 수는 인자로 자기 값을 넘긴다(§4.1-A).
- **차트(Altair, `st.altair_chart`)**: 최근 `GRAPH_TREND_HOURS`(**3시간**) 라인 + **상태 임계선**(경고/위험/고장, 상태색 점선). 원본을 그대로 그리므로 원형 마커는 없다(`show_points=False`).
  - **창은 6 → 3시간이다 (2026-08-12 사용자 요청).** 원본 표시로 바꾼 뒤 6시간을 340px 폭에 밀어 넣으니 선이 굵은 띠처럼 뭉쳤다. 창을 줄이면 같은 폭에 절반의 시간이 들어와 변화가 벌어져 보이고 점 수도 절반이 된다(실측: 10대 기준 선분 41,980 → 18,620, 한 차트 최대 2,027 → 895). **모터 세부정보는 6시간(`DETAIL_TREND_HOURS`)을 유지한다** — 한 대를 파고드는 화면이라 이력이 더 쓸모 있다.
  - **X축 눈금은 30분 고정(`GRAPH_X_TICK_MINUTES`)**: 3시간 창에서 Vega 자동 선택(1시간)은 격자가 두세 줄뿐이라 값이 언제 변했는지 짚기 어렵다. 실측 라벨 `13:30 · 14:00 · 14:30 · 15:00 · 15:30 · 16:00`.
    - **구현 주의**: Vega-Lite 문법상 맞는 `tickCount={"interval":"minute","step":30}`은 **이 번들의 Vega에서 렌더를 통째로 깨뜨린다**(실측: 차트 40개 전부 빈칸, 콘솔 `Cannot read properties of undefined (reading 'bounds')`). 대신 데이터 구간을 30분으로 나눈 **개수**를 `tickCount`에 넘기면 Vega가 나이스 시간 단위 중 30분을 골라 같은 결과가 된다.
  - **축(2026-08-06 최종)**: X축(시간 HH:MM)·Y 숫자 눈금·축선·격자를 **모든 차트에 동일하게** 표시한다. 이 페이지의 모든 차트가 같은 축 구성·높이(`GRAPH_CHART_HEIGHT_PX` 130px)·Y범위를 써 세로 축척이 같으므로 서로 다른 모터를 눈으로 비교할 수 있다. (이전의 "Y축은 한 번만/임계선을 Y기준으로" 결정은 폐기했다.)
  - **데이터는 다운샘플 없이 원본 그대로다 (2026-08-12 사용자 요청).** 종전 15분 구간 평균(`GRAPH_TREND_BUCKETS`)은 스파이크를 지워 임계선을 넘은 적이 없는 것처럼 보이게 했다 — 실측 근거는 §4.1-A. 상세와 같은 `get_motor_metric_raw_series`를 쓰고 `show_points=False`로 넘긴다.
  - **비용 실측**: 창을 3시간으로 줄인 뒤 10대 기준 선분 **18,620개**(한 차트 최대 895), 차트 40개 렌더 **1,484ms**. 6시간이던 시점의 값 — 20대: 선분 52,712 · 쿼리 15ms · spec 1,195ms · 4.1MB · 렌더 1,343ms / 10대: 선분 41,980 · spec 689ms · 3.2MB · 렌더 1,638ms. 모터마다 수집 주기가 10~60초로 달라 대수에 정비례하지 않는다.
  - **되돌릴 여지**: `get_motor_metric_series`(구간 평균)와 `GRAPH_TREND_BUCKETS`는 지운 게 아니라 남겨 뒀다. 원본이 무겁다고 판단되면 이 페이지만 평균으로 되돌린다.
  - **높이는 이 페이지에서만 130px이다 (2026-08-12).** 모터 세부정보(§4.1-A)는 한 대만 보므로 260px를 쓴다. 여기서 키우지 않는 이유 — 최대 20대 × 4지표 = 80개 차트를 세로로 쌓으므로 스크롤이 그대로 곱절이 되고, 나열된 모터를 같은 세로 축척으로 비교한다는 전제가 깨진다.
  - **Y 눈금**: 지표별 **나이스 올림 상한**(온도·소음 100, 진동 10, 전류 25)까지 **2단계**(`GRAPH_CHART_Y_DIVISIONS`) = `[0, 중간, 최상단]` 3눈금만 둔다 — 130px에 더 넣으면 라벨이 겹친다. 세부정보(260px)는 10단계다(§4.1-A). 라벨은 `format=".2~f"`(12.5가 "13"으로 반올림되지 않게. 소수 2자리인 이유는 §4.1-A 참조).
    - 축 API는 **눈금 개수가 아니라 단계 수**로 받는다(`_y_ticks(fault, divisions)`, 눈금 = 단계 + 1). 상한을 등분해야 값이 나이스 숫자로 떨어지기 때문이다 — 10단계면 전류가 0·2.5·…·25지만, "눈금 10개"로 잡으면 9등분이라 2.78·5.56이 된다.
  - **구현 주의**: 축은 `axis=None` 대신 `alt.Axis(...)`로 준다 — 일부 Streamlit/Vega 버전이 `axis=None`에서 렌더 크래시(`forEach of undefined`)한다. `.configure_*()`는 Streamlit이 렌더하지 못하므로 쓰지 않는다.
- **표시 범위 셀렉트박스**: 검색 타이핑을 막고 드롭다운 선택만 허용한다(입력을 `readOnly`로, 캐럿 숨김 — CSS로는 키 입력을 못 막아 컴포넌트 스크립트로 부모 DOM에 `readOnly` 적용).
- **데이터**: 목록은 `list_company_motor_status()`(지표별 상태 배지도 여기서 나온다), 시계열은 `get_motor_metric_raw_series()`(모터당 4지표 한 쿼리, 다운샘플 없음, 최근 `GRAPH_TREND_HOURS` 3시간).
- **회사 격리**: `session_state.company_id` 기준. 빈 회사/빈 결과는 안내 문구.

## 3-B. 모터 현황 페이지 (2026-08-06 추가)

> 재정리안 2페이지. 그룹핑 방식을 골라 전체 모터를 카드로 조망하는 화면.

- **검색(`st.text_input`, 2026-08-12 추가)** — 모터 **이름 또는 `motor_id`** 부분 일치(대소문자 무시)로 목록을 좁힌다. 검색 중에는 `'{검색어}' 검색 결과 N대` 캡션을 띄우고, 일치가 없으면 안내 후 `st.stop()` 한다.
  - 도입 근거: 200대를 카드로 훑어 특정 호기를 찾는 것은 불가능하다는 고객 의견(`.claude/docs/review/2026-08-12_customer-pm-opinion.md` MVP 1-(1)).
- **조작부는 한 줄에 나란히, 순서는 그룹핑 → 검색, 각각 1/4 폭 (2026-08-12 사용자 요청).** 둘 다 "무엇을 볼지"를 정하는 조작부라 위아래로 쌓으면 카드가 그만큼 아래로 밀린다. `st.columns([1, 1, 2])`(`_CONTROL_COLUMNS`)로 나누고 **마지막 2/4는 비워 둔다** — 조작부가 전체 폭을 차지하면 카드 그리드와 무게가 비슷해져 어느 쪽을 먼저 봐야 하는지 흐려진다.
  - 라디오에는 `width="stretch"`를 준다 — 그대로 두면 내용 폭으로 줄어(실측 202px) 옆 검색칸과 어긋난다.
  - **두 박스는 테두리·배경·모서리·여백이 같아야 한다.** `ui/styles.py`가 `.st-key-status_grouping_mode, .st-key-status_search`를 한 벌로 스타일한다(`1px solid border` · `radius 10px` · `padding 8px 14px` · `surface` 배경 · `display: block`). 종전에는 그룹핑에만 박스가 있고 검색은 테두리 `0px`·배경 투명이었다.
  - 적용 후 실측(뷰포트 1280, 콘텐츠 폭 1270): **그룹핑 x=80→347 · 검색 x=363→630 · 둘 다 267px · 같은 행(y=299) · 네 스타일 속성 일치**. 컬럼 비율은 정확히 1/4씩이지만 컬럼 사이 간격 때문에 실제 렌더 폭은 콘텐츠 폭의 **21%**다.
  - **`ui/styles.py`를 고치면 서버를 재시작해야 한다.** 전역 CSS는 캐시되어 브라우저 새로고침이나 스크립트 재실행만으로는 갱신되지 않는다 — 이 변경에서 실제로 옛 스타일이 그대로 보였다.
- **그룹핑(라디오 버튼)** — 값·순서·기본값은 config(=환경설정)에서 관리(`GROUPING_MODES`, 기본 `DEFAULT_GROUPING_MODE`). 표시 순서와 라벨(2026-08-12 변경):
  - **상태별 정보**(기본 선택): `FAULT → DANGER → WARNING → NORMAL`(`STATUS_GROUP_ORDER`)
  - **위치별 정보**: `installation_location` 기준, 위치명 오름차순
  - **"확인 사항 정보"를 뺐다 (2026-08-12 사용자 요청).** 종전 기본값이었고 `FAULT → DANGER → WARNING`(NORMAL 제외)이었는데, 상태별 정보와 답하는 질문이 같고 NORMAL만 빠진 부분집합이라 옵션 셋 중 둘이 겹쳤다. `GROUPING_MODE_ISSUE`·`ISSUE_GROUP_ORDER` 상수도 함께 삭제했다.
  - 이 삭제로 **검색이 NORMAL을 되살려야 하던 예외도 사라졌다.** 종전 기본 그룹핑이 NORMAL을 빼고 있어(210대 중 151대) 정상 설비를 검색하면 빈 화면이 나오는 함정이 있었고, 검색 중에는 그 제외를 풀고 캡션으로 알렸다. 이제 어느 그룹핑에서든 정상 설비가 그대로 검색된다.

  - **[추후 추가] 수명별 그룹핑 (2026-08-13).** `motors.lifespan_hours` · `operation_started_at`이 들어왔으므로(`04 §3.3`) 경과율 구간(예: `수명 초과` / `80% 이상` / `그 외`)을 그룹핑 옵션에 넣을 수 있다. **이 화면이 상세(§4.1) 다음 자리다** — 200대 중 교체가 임박한 설비를 한 번에 모아 보는 것은 이 페이지에서만 가능하다. 구간 경계값은 config로 뺀다(임계값·색상과 같은 이유). 선행 조건과 상세는 `remaining_work` #22.

  그룹 헤더는 `라벨 (N대)` 형식으로 표기한다(상태 라벨에 대괄호는 쓰지 않는다 — 예: `제1공장 1층 유틸리티실 (17대)`, `FAULT (4대)`).
- **그룹 헤더** 아래 카드를 가로 `STATUS_CARDS_PER_ROW`(기본 7)개씩 배치. 빈 그룹은 표시하지 않는다.

**카드 폭 확정 (2026-08-07) — 10개 128px → 7개 170px.** 한 줄 10개에서는 지표 줄이 잘렸다. 200대 전체 400개 지표 줄이 **전부** 넘쳤고 최대 24px가 화면 밖으로 나가, `온도 82.2 °C · 진동 3.6 mm/s`에서 뒤쪽 단위가 사라졌다.

**모터명은 말줄임을 허용하지만 지표 값은 잘리면 안 된다.** 담당자가 카드를 훑는 목적이 수치 확인이기 때문이다. 200대 전체를 대상으로 잘림 수를 실측했다.

| 한 줄 카드 수 | 카드 폭 | 여백 압축 없음 | 여백 압축 적용 |
|---|---|---|---|
| **7개** | 170px | **잘림 0** | 잘림 0 |
| 8개 | 148px | 잘림 200줄 | 잘림 0 |
| 9개 | 132px | 잘림 400줄 | 잘림 219줄 |
| 10개(기존) | 128px | 잘림 400줄 | — |

- **9개 이상은 불가능하다.** 여백을 최대로 줄여도 400줄 중 219줄이 잘린다.
- 8개도 가능하지만 여백을 압축해야 겨우 맞아 답답하다. **7개로 확정**해 여백을 원래대로 두고 카드에 숨통을 준다.
- **7개 배치의 카드 폭 상한은 172px**다(`7×172+60=1264` ≤ 컨테이너 1270px, 1440 뷰포트 기준). `config.STATUS_CARD_MIN_WIDTH_PX`를 170px로 두면 행 폭 1250px로 20px 여유가 남는다.
- 부수 효과로 **모터명도 말줄임 없이 전부 들어간다** — 모터명 최대 필요 폭이 147px인데 내용 폭이 147px다.

**여백은 균일한 `gap`이 아니라 자리별로 준다.** 균일하게 줄이면 `온도82.2`처럼 라벨과 값이 붙어 한 단어로 읽힌다.

| 자리 | 값 | 근거 |
|---|---|---|
| 라벨 → 값 (`온도` `82.2`) | 4px | 10px 글꼴의 공백 한 칸. 붙으면 한 단어로 읽힌다 |
| 값 → 단위 (`82.2` `°C`) | 2px | 붙어도 수치와 단위로 구분되어 읽힌다 |
| 지표 사이 구분자 (`·`) | 좌우 3px | — |
| 요소 간격 (`sc-metric-line` gap) | 3px | — |

**세 자리 값 대비.** 온도는 100도를 넘을 수 있다. 내용 폭 147px 기준 실측 여유는 다음과 같다.

| 케이스 | 필요 폭 | 여유 |
|---|---|---|
| 현재 데이터 최대 | 131px | 16px |
| 온도 3자리 (`120.5 °C`) | 137px | 10px |
| 4개 지표 전부 3자리 | 142px | 5px |

`.sc-metric-line`에 `overflow: hidden; text-overflow: ellipsis`를 유지해, 이보다 긴 값이 와도 레이아웃이 밀리지 않고 말줄임으로 끝나게 한다.
- 카드 폭은 고정이다(`flex: 0 0`) — 남는 가로 공간을 채우지 않고 열 수만 화면 폭에 따라 준다. 모든 카드가 부분 행에서도 정확히 같은 크기여야 하기 때문이다.
- **카드(`status_card`)**: 이름 + 상태 배지, 설치 위치, 모델명, `온도·진동`/`전류·소음` 최신값, "N시간 전 상태변경". **카드 전체가 클릭 영역**이며 클릭 시 상세 페이지로 이동한다. 상태 색상은 기존 `status-{status}` 팔레트를 그대로 쓴다.
- **경량 카드인 이유**: 200대를 한 화면에 그리므로 인라인 SVG·게이지·흐름 애니메이션을 가진 §3.2 `motor_card`를 재사용하면 렌더 비용이 감당되지 않는다. 텍스트 위주 카드를 별도로 두되(오버레이 클릭 기법은 동일) 상태색만 공유한다.
- **데이터**: `list_company_motor_status()` 단일 배치 조회(윈도우 함수, 모터 수와 무관하게 고정 쿼리 수).

## 4. 모터 상세 페이지

### 4.0 제목 + 상태별 지표 배지 (2026-08-11 사용자 요청)

제목 오른쪽에 **상태마다 배지 하나**를 두고 괄호 안에 해당 지표를 적는다 (`components.detail_header()`).

```
2호기 메인 송풍기   [DANGER (온도)]  [WARNING (진동)]
137호기 원심분리기 모터   [FAULT (소음)]
```

**두 가지를 함께 고쳤다.**

- **어느 지표가 어느 상태인지 밝힌다.** 종전에는 대표 상태 배지 하나(`DANGER`)만 띄워, 담당자가 무엇 때문에 위험한지도 무엇을 먼저 봐야 하는지도 알 수 없었다. 상태는 여러 개가 동시에 나올 수 있으므로 배지도 여러 개다.
- **제목 바로 옆에 붙인다.** 종전에는 `st.columns([4, 1])`로 나눠 배지가 화면 오른쪽 끝에 갔다 — 1600px 폭에서 제목과 1,000px 넘게 떨어져 **둘이 같은 설비를 말한다는 것이 읽히지 않았다.** 모터 카드의 `motor-head`와 같이 한 덩어리(`.detail-head`)로 그린다. 실측 간격 14px, 세로 중심 차이 0.0px.

규칙:

- 배지 순서는 심각도 내림차순(`STATUS_GROUP_ORDER`), 괄호 안 지표는 카드와 같은 고정 순서(`METRIC_NAMES`). 화면마다 순서가 다르면 눈이 다시 찾아야 한다.
- 이상이 하나도 없으면 `NORMAL` 하나만 둔다.
- 괄호 안 지표는 상태명보다 한 단계 약하게(opacity 0.85) 둬 `DANGER`가 먼저 읽히게 한다.
- **미확인 FAULT는 수치가 내려가도 FAULT로 표시한다** (03 §4.3) — 담당자가 정비 완료를 확인하기 전까지는 고장으로 본다.
- 제목은 `st.title()`이 아니라 마크다운 `<h1>`이다. 배지와 같은 flex 줄에 넣으려면 한 요소 안에 있어야 하며, Streamlit이 마크다운 `h1`에도 제목 타이포그래피를 적용하므로 크기·굵기는 달라지지 않는다.

지표별 상태는 `services/motors.get_metric_status_view()`가 (지표별 상태, 미확인 FAULT, 대표 상태) 셋을 **한 번에** 돌려준다. 종전 상세 페이지는 `get_representative_status()`와 `find_unconfirmed_fault_metrics()`를 따로 불러 같은 쿼리를 두 번씩 돌렸다 — 자동 갱신으로 10초마다 반복되는 경로다.

### 4.1 기본 정보

- **[← 대시보드] 버튼은 상단 내비로 대체한다 (2026-08-10 변경).** 이제 상세 페이지에도 §5-4의 3버튼 내비(메인 대시보드 / 모터 그래프 / 모터 현황)를 그린다. 종전에는 `← 대시보드` 하나뿐이라 모터 그래프로 가려면 대시보드를 한 번 거쳐 두 번 눌러야 했다. 상세는 내비에 없는 하위 화면이라 **어느 버튼도 강조하지 않는다**(`page_header()`를 인자 없이 호출).
  - 모터 미선택·조회 실패 화면의 `대시보드로 이동` 버튼은 그대로 둔다 — 그 상황에서는 안내와 함께 나가는 경로가 필요하다.
- 항목: 모터 ID, 설치 위치, 모델명, 수집 주기, 시리얼 번호, 등록일자 (`motors` 테이블)
- **[추후 표시] 모터 수명 — 이 표가 1순위 자리다 (2026-08-13 사용자 요청).** `motors.lifespan_hours`(설계 수명, 시간)와 `motors.operation_started_at`(실제 가동 시작일) 컬럼은 이미 있고 시연 데이터 210대도 채워져 있지만(`04 §3.3`), **아직 어느 화면에도 나오지 않는다.** 화면에 붙일 때의 규칙:
  - **넣을 항목**: `구동일자` · `설계 수명` · `경과율(잔여 수명)`. 앞의 둘은 원본 값이고 셋째는 계산값이다.
  - **`등록일자`와 `구동일자`를 나란히 두되 이름으로 구분한다.** 등록일자는 모니터링 서비스에 등록한 날, 구동일자는 설비가 실제로 돌기 시작한 날이라 값이 다르다(구동일자가 앞선다). 두 날짜가 이유 없이 다르게 보이면 담당자가 어느 쪽이 맞는지 묻게 되므로, 항목명만으로 구분되지 않으면 캡션을 붙인다.
  - **경과율은 서비스 레이어에서 한 번만 계산한다** (`app/services/motors.py`). 상세·현황·리포트가 각자 계산하면 같은 모터에 다른 숫자가 찍힌다 — 임계값에서 이미 같은 결함을 겪었다(`04 §2` 각주).
  - **값이 NULL일 수 있다.** 관리자 화면 등록 폼에 두 필드가 없어(§6.3) 지금 등록된 모터는 비어 있다. `-`로 적고 표를 깨뜨리지 않는다. 시리얼 번호가 이미 같은 방식이다(`components.motor_info_table()`).
  - **선행 조건**: 가동시간 산출 규칙(24시간 연속으로 볼지, 가동률을 둘지)이 정해져야 경과율을 적을 수 있다. 상세는 `remaining_work` #22.
- **표로 표시한다 (2026-08-10 변경).** `components.motor_info_table()`이 한 줄에 두 쌍(항목·값·항목·값)을 넣어 종전 2열 배치의 밀도를 유지한다. 항목명 칸은 배경(`surface_muted`)으로 구분한다.
- **왜 바꿨나.** 종전에는 `st.write("**항목** 값")`을 두 열로 흩뿌려, 항목명과 값이 한 줄에서 굵기만 다르게 붙어 있어 어디까지가 이름인지 눈으로 잘라 읽어야 했다. 게다가 바로 아래 §4.2가 `st.dataframe`이라 **한 페이지에 생김새가 다른 표가 둘** 보였다. 두 섹션이 같은 테두리·같은 셀 여백을 쓰는 표(`.detail-table`)를 공유한다.

### 4.1-A 모터 그래프 (2026-08-10 추가)

- **§3-A 모터 그래프 페이지와 같은 차트**를 이 모터 한 대에 대해 보여준다. 지표 4열(온도/진동/전류/소음), 열 헤더에 임계값 범례, 최근 `DETAIL_TREND_HOURS`(6시간) 추이, 경고·위험·고장 점선 — **창 길이는 그래프 페이지(3시간)와 다르다**.
- **위치: 기본 정보(§4.1)와 지표별 임계값(§4.2) 사이.** 정비 완료 확인(§4.3)보다는 아래에 둔다 — 그래프가 위로 오면 설비가 멈춘 상태에서 가장 급한 조치 버튼이 화면 아래로 밀린다.
- **모터명·상태 배지 줄은 넣지 않는다** (`show_motor_row=False`). 한 대뿐이라 같은 이름이 네 번 반복될 뿐이고, 페이지 제목과 그 옆의 상태별 지표 배지(§4.0)가 이미 알려준다.
- **차트 코드는 `app/ui/charts.py`로 분리해 두 화면이 공유한다.** 종전에는 `app/pages/motor_graph.py` 안에만 있어 재사용할 수 없었다. 축 구성·임계선·Y범위 규칙이 **한 벌만** 존재해야 두 화면의 그래프를 같은 눈으로 읽을 수 있다.
- **데이터는 다운샘플 없이 원본 그대로 그린다 (2026-08-12).** `get_motor_metric_raw_series(conn, motor_id, DETAIL_TREND_HOURS)` — 종전의 15분 구간 평균이 아니다.
  - **왜**: 구간 평균은 스파이크를 지운다. 실측(137호기) — 한 15분 구간에서 온도 원본 최대 **38.27 → 평균 33.14**(−13%), 소음 **108.96 → 105.33**. 전체 19,920개 구간 중 **18개(0.1%)** 는 평균과 원본 최대의 **상태 구간 자체가** 달랐다(예: 평균 74.49 경고 / 원본 75.12 위험). 상태 판정은 원본 최신값(`get_latest_metric_statuses`)을 쓰므로, 그래프만 평균이면 배지와 그래프의 근거가 어긋난다.
  - **비용 실측**: 지표당 2,026점(6시간 × 10초 수집). 쿼리 3.0ms(버킷 1.0ms), 차트 4개 spec 생성 156ms, 전송 JSON 630KB, 대시보드 클릭 → 차트 4개 렌더 완료 **1,599ms**. 10초 자동 갱신에서 레이아웃 붕괴·서버 오류 없음.
  - **포인트 마커는 끈다**(`show_points=False`). 점이 2천 개면 선이 마커로 덮인다.
  - **그래프 페이지(§3-A)도 같은 날 원본으로 바꿨다** — 두 화면의 값이 같은 근거를 갖는다. 그쪽 비용 실측은 §3-A 참조.
- **세로 크기와 Y축 단계 수도 이 화면 전용 값이다 (2026-08-12 사용자 요청).** 차트 높이 `DETAIL_CHART_HEIGHT_PX` **260px**(그래프 페이지의 2배), Y축 `DETAIL_CHART_Y_DIVISIONS` **10단계**(눈금 11개). `metric_graph_grid(..., height=, y_divisions=)` 인자로 넘긴다 — 기본값은 그래프 페이지 값이라 `motor_graph.py`는 그대로다.
  - **왜 바꿨나**: 130px에서는 6시간 창의 완만한 값 변화가 거의 수평선으로 보여 추이가 읽히지 않았다. 한 대만 보는 화면이라 세로를 2배로 써도 스크롤 부담이 적다.
  - **Y축은 4단계 → 10단계로 두 번 늘렸다.** 실측 눈금 — 온도·소음 `0·10·…·100`, 진동 `0·1·…·10`, 전류 `0·2.5·…·25`. 라벨 포맷은 `.1~f` → **`.2~f`**(4단계 시절 전류의 6.25·18.75가 6.3·18.8로 반올림돼 격자선과 어긋났다. 그래프 페이지의 2단계 값 50·5·12.5는 표기가 그대로라 영향 없음).
  - **라벨이 절반만 찍히던 문제 (2026-08-12 실측)**: 10단계를 줬는데 화면에는 `0·20·…·100` 6개만 나왔다. 격자선은 11줄 그대로였고 spec의 `values`도 11개였다 — Vega의 **라벨 솎기**였다. `labelOverlap=False`만으로는 듣지 않았고, **임계선 레이어(`mark_rule`)에도 같은 축 설정을 준 뒤에야** 11개가 모두 찍혔다. 레이어가 같은 Y 스케일을 공유하면 Vega가 축을 하나로 합치는데, 한쪽 레이어를 비워 두면 기본 축 설정이 섞여 들어온다.
- **차트마다 지표별 상태 배지를 붙인다 (2026-08-12 사용자 요청, `show_metric_status=True`).** 모터 그래프 페이지(§3-A)와 같은 표시다. 제목 옆 배지(§4.0)가 "FAULT (소음)"으로 이미 알려주지만, 그래프를 보는 동안 눈이 화면 위쪽으로 돌아가지 않아도 되게 차트 위에 다시 붙인다. 상세는 `get_metric_status_view()`의 `metric_statuses`·`fault_metrics`를 그대로 넘긴다.
- 표시 범위 필터(상태/위치/모델명/최대 수)는 두지 않는다 — 대상이 이미 이 모터 하나로 정해져 있다.

### 4.2 지표별 임계값 표시

- 온도/진동/전류/소음 각각 정상/경고/위험/고장 구간을 표로 표시
- 데이터 출처: `motor_thresholds` (`04_database_schema.md` §2에서 신설된 테이블, 지표별로 4행)
- **구간 값을 상태색으로 칠한다 (2026-08-10 변경).** `components.threshold_table()`. 네 구간이 전부 같은 글씨색이면 어느 값이 위험한 구간인지 표를 위아래로 훑어야 알 수 있다. 색이 곧 심각도다 — 팔레트는 §5-3의 단조 증가 램프(녹색 → 앰버 → 주황 → 빨강)를 그대로 쓰고, 테마별 값(`config.THEMES[...]["status"]`)이라 다크에서도 대비가 확보된다.
- 머리글도 같은 상태색으로 물들여 **열과 색의 대응**을 한 번에 알려준다. 열 순서는 심각도 오름차순(`_THRESHOLD_COLUMNS`)이라 왼쪽에서 오른쪽으로 읽으면 그대로 악화 순서다.
- 머리글 표기는 `STATUS_KOREAN_LABELS`(정상/경고/위험/고장)를 쓴다 — 리포트의 임계값 표(`06 §2.2`)와 같은 어휘다.
- `st.dataframe`을 쓰지 않는다: 셀 단위 색을 넣을 수 없고, 위 §4.1 표와 생김새가 달라진다.
- 표 아래 `이 설비에 설정된 기준값입니다. 모터마다 다르게 설정될 수 있습니다.` 캡션을 둔다(리포트와 같은 문구).

### 4.3 FAULT 정비 완료 처리 (신규 — `03_state_event_logic.md` §8-3 반영)

- 모터의 특정 지표가 FAULT이고 아직 관리자 확인 전(§3 참고: 최신 로그가 FAULT이고 `contact_id`가 NULL)인 경우, **"정비 완료 확인"** 버튼 노출
- 버튼 클릭 시:
  1. 로그인한 담당자의 `contact_id`로 `motor_status_logs`에 신규 행 기록 (`trigger_reason`: "관리자 정비완료 확인", `contact_id` 채움)
  2. 이후부터 해당 (모터, 지표)의 자동 상태 판정 재개
- 버튼은 실수 클릭 방지를 위해 확인 다이얼로그(예: `st.dialog` 또는 체크박스+버튼 조합) 동반 권장 (보강)

**구현 확정 (2026-08-05)**

- **미확인 FAULT 판정**: 지표별 **최신** `motor_status_logs` 행의 `new_status`가 FAULT이면 미확인으로 본다. 정비 완료 확인이 같은 지표에 새 행을 남기므로, 확인이 끝나면 최신 행이 FAULT가 아니게 되어 자연히 해제된다. (`contact_id IS NULL` 조건을 따로 보지 않아도 같은 결과가 된다.)
- **신규 행의 상태값**: `previous_status = 'FAULT'`, `new_status = 'NORMAL'`. "정비가 끝나 정상 판정부터 다시 시작한다"는 뜻이며, 이후 수집값이 임계를 넘으면 평소대로 전이가 감지된다.
- **대표 상태와의 관계**: 미확인 FAULT 지표가 하나라도 있으면 최신 텔레메트리 값과 무관하게 모터 대표 상태를 **FAULT로 유지**한다 (`03_state_event_logic.md` §4.3).
- **다이얼로그 상태 유지**: 확인 대상을 세션 상태에 담아둔다. 버튼 클릭 여부로만 다이얼로그를 열면 그 안의 체크박스를 누르는 순간 rerun이 일어나 다이얼로그가 닫히고 확인 절차를 마칠 수 없다. 확인·취소 시 세션 키를 지운다.
- **확인 단위는 지표가 아니라 설비 (2026-08-05 변경)**: 한 모터에 FAULT 지표가 여럿이면 지표마다 확인을 반복하게 되는데, 현장에서 "이 설비 정비를 마쳤다"는 것은 한 번의 행위다. 다이얼로그에 대상 지표를 모두 나열하고 한 번의 확인으로 각 지표에 로그를 1건씩 적재한다.
- **대시보드에서도 확인할 수 있다 (2026-08-05 추가)**: §3.2 모터 카드에 같은 버튼을 둔다. 설비가 멈춘 상태에서 가장 급한 조치가 상세 페이지 안에만 있으면 담당자가 두 번 이동해야 한다. 버튼과 다이얼로그는 두 화면이 같은 컴포넌트를 공유한다.

### 4.4 이벤트 발생 내역 리스트

**§3.3 목록과 완전히 같게 맞춘다 (2026-08-10 확정).** 화면만 다르고 목록 자체는 하나의 규칙을 따른다 — 담당자가 대시보드에서 보던 표를 상세에서 다시 읽는 법을 배우지 않아도 된다.

| 항목 | 규칙 |
|---|---|
| 컬럼 | 발생 일시, **모터명**, 상태 변화, **값 변화**, 발생 사유, 리포트 버튼 — §3.3과 동일 |
| 열 폭 | `config.EVENT_COLUMN_WIDTHS_WITH_MOTOR` — §3.3과 같은 상수 |
| 결과 상태 필터 | `config.DASHBOARD_EVENT_STATUSES`(FAULT·DANGER) — §3.3과 동일 |
| 컴포넌트 | `event_list_header(show_motor=True)` / `event_row(..., show_motor=True)` |
| 데이터 범위 | 해당 모터의 이벤트만 (`list_motor_events(..., statuses=...)`) |
| 페이징 | 페이지당 20개 (`DETAIL_EVENT_PAGE_SIZE`) — 이 화면만의 추가 기능 |
| 캡션 | `고장 · 위험 전이 N건 · [x/y 페이지 ·] 이 모터의 이력만 모아서 보여줍니다.` |

**종전에 어긋나 있던 것 (2026-08-10 정정).** 컬럼이 5개(모터명 없음), 열 폭 상수가 달랐고(`EVENT_COLUMN_WIDTHS`), 상태 필터를 걸지 않아 `NORMAL → WARNING` 전이가 섞였다. 그 행들은 리포트가 없어 마지막 칸이 비었고, 캡션도 없어 같은 이름의 목록이 두 화면에서 다르게 보였다.

**모터명 열을 남긴 이유.** 이미 그 모터의 페이지이므로 정보로서는 중복이다. 그래도 §3.3과 열 구성·폭이 완전히 같아야 두 목록이 같은 표로 읽힌다 — 일관성을 택했다.

**필터를 걸면서 잃은 것.** 관찰 단계(`NORMAL → WARNING`) 전이는 이제 어느 화면에도 나오지 않는다. 종전 §4.4는 "악화 과정을 보는 이력"이었고 §3.3 캡션이 "전체 이력은 상세에서"라고 안내했는데, 두 서술 모두 이번에 걷어냈다(§3.3 캡션은 "모터별 이력은 상세 페이지에서"로 변경). 관찰 단계 이력이 다시 필요해지면 필터를 빼는 한 줄 변경으로 되돌릴 수 있다.

- 리포트 버튼의 노출 조건과 클릭 동작은 §3.3과 동일하다 (DANGER/FAULT 로그에 노출, 화면에는 항상 HTML 표시 + 가능한 형식으로 다운로드). 필터가 같아지면서 **모든 행에 버튼이 있다.**
- 총 건수는 페이지네이션 유무와 무관하게 캡션이 항상 말한다. 페이지 표시(`x / y`)는 현재 위치만 담당한다.
- `count_motor_events()`와 `list_motor_events()`는 **같은 필터**를 받아야 한다(`_motor_event_filter` 공유) — 다르면 페이지 수와 실제 행 수가 어긋난다.

## 5. 확정 사항 (보강/제안 항목)

### 5-1. 로그인 시 MAC 주소 체크 → IP 체크만 적용 (확정)

MAC 주소는 일반 웹 브라우저 요청에서 서버가 확인할 방법이 없어(HTTP 프로토콜 자체의 구조적 제약) 요구사항에서 제외. `company_contacts.allowed_ip` 기반 IP 체크만 적용 (§2 반영 완료).

### 5-2. 실시간 그래프 자동 갱신 방식 → st.fragment(run_every=...) 적용 (확정)

최초 제안한 `streamlit-autorefresh`는 내부적으로 `st.rerun()`을 호출해 **스크립트 전체를 재실행**하는 방식이라 페이지가 커질수록 갱신마다 불필요하게 느려지는 문제가 있음. 대신 Streamlit 1.33+ 내장 기능인 **`st.fragment(run_every="10s")`**로 확정 — 차트/카드 등 갱신이 필요한 영역만 함수 단위로 분리해 해당 부분만 부분 재실행되도록 함(로그인 체크, DB 조회, 다른 카드 렌더링 등 나머지 페이지는 재실행되지 않음). 별도 패키지 설치 불필요. **`01_tech_stack.md` §2.5에도 반영 완료.**

#### 5-2-1. 구현 (2026-08-11)

**적용 화면은 메인 대시보드와 모터 상세 2곳이다** (사용자 확정). 위 결정은 방식만 정하고 어느 화면인지는 정하지 않았었다. 페이지별 조회 비용은 어디든 작아(0.3~13ms) 판단 기준은 **브라우저 렌더 부담과 화면 성격**이었다.

| 화면 | 조회 | 판단 |
|---|---|---|
| 메인 대시보드 | 12.8ms / 226회 | **적용** — "지금 무슨 일이 있나"에 답하는 상시 감시 화면 |
| 모터 상세 | 0.3ms / 2회 | **적용** — 가장 가볍고, 커레이션 모터(10/20/30초)를 열면 초 단위로 자라는 것이 보인다 |
| 모터 그래프 | 12.2ms / 213회 | 제외 — 10초마다 Altair 차트 40개(10대 × 4지표) 재렌더 |
| 모터 현황 | 5.7ms / 203회 | 제외 — 카드 200장 재렌더가 스크롤·그룹핑 조작을 방해할 수 있다 |

**한 fragment로 묶는다.** 대시보드의 요약·배너·카드·이벤트는 같은 조회 결과(`motors`)를 공유하므로 나누면 조회가 네 번이 되고, 갱신 시점이 어긋나 상단 요약과 카드가 서로 다른 순간을 말하게 된다.

**헤더·내비게이션·다이얼로그는 fragment 밖에 둔다.** 갱신할 이유가 없고, 다이얼로그는 갱신 때마다 다시 그리면 열려 있는 창이 닫힌다. fragment 안의 버튼(리포트 보기·정비 완료 확인·이벤트 페이지 이동)은 `st.rerun()`의 기본 scope가 `app`이라 바깥 다이얼로그를 정상적으로 연다.

**데이터도 함께 흘러야 한다.** 시드는 실행 시각까지만 채우므로 갱신만으로는 숫자가 그대로다. 런타임 틱이 경과분을 이어 붙인다 — `02_architecture.md §6.1.2`.

#### 5-2-2. 갱신 카운터 (2026-08-11 사용자 요청)

자동 갱신이 돌아도 화면이 그 사실을 말하지 않으면, 값이 그대로인 것이 "아직 갱신 전"인지 "화면이 멈춤"인지 담당자가 구분할 수 없다. `components.refresh_countdown()`이 **`● 다음 갱신까지 N초`** 한 줄을 보여준다.

**뷰포트 우하단에 고정한다 (2026-08-11 2차 요청).** 처음에는 fragment 맨 위, 문서 흐름 안에 두고 남은 초·진행 막대·마지막 갱신 시각을 한 줄로 늘어놓았다. 그런데 대시보드는 카드 20장 + 이벤트 목록이라 **스크롤을 내리면 카운터가 화면 밖으로 나갔다** — 정작 값이 바뀌는 카드를 보는 동안 카운터가 눈에 없었다. 지금은 `st.container(key=...)`로 감싸 `st-key-{key}` 클래스를 만들고 styles.py가 `position: fixed`로 띄운다(모터 카드 오버레이와 같은 기법). 표시도 한 줄로 줄였다 — 떠 있는 배지는 작아야 방해가 없다. 진행 막대는 없앴고 마지막 갱신 시각은 툴팁으로 남겼다.

| 항목 | 값 | 근거 |
|---|---|---|
| 위치 | **좌하단** — `REFRESH_COUNTDOWN_LEFT_PX` / `REFRESH_COUNTDOWN_BOTTOM_PX` (20) | 네 모서리 중 좌하단만 비어 있다 (아래 참조) |
| z-index | `REFRESH_COUNTDOWN_Z_INDEX` (60) | 본문 위·모달 아래. 리포트·정비확인 창을 가리면 안 된다 |
| 크기 | `REFRESH_COUNTDOWN_WIDTH_PX` × `REFRESH_COUNTDOWN_HEIGHT_PX` (150 × 26) | 카드 위에 떠도 내용을 거의 가리지 않는 최소 크기 |
| 고정 기준 | `REFRESH_COUNTDOWN_KEY` — `st.container(key=...)`가 만드는 `st-key-{key}` 클래스 | styles.py가 이 클래스를 `position: fixed`로 잡는다 |
| 갱신 간격 | `REFRESH_COUNTDOWN_TICK_MS` (100) | 초 표시가 매끄럽고 부하는 무시할 수준 |

실측: 스크롤 4000px를 내려도 배지 좌표가 불변, 대시보드·모터 상세 모두 동일. 다이얼로그를 열면 배지 좌표의 최상위 요소가 모달 쪽으로 바뀌어(z-index가 의도대로 아래) 창을 가리지 않는 것도 확인했다.

**우하단 → 좌하단으로 옮겼다 (2026-08-12 사용자 확인).** 배포본에서 **Streamlit Cloud의 `Manage app` 버튼이 같은 자리에 떠 배지를 통째로 가렸다.** 로컬에는 그 버튼이 없어 그동안 드러나지 않았고, 자동 갱신이 도는지 알려주는 유일한 표시가 배포본에서만 사라져 있었다. 네 모서리 중 남는 곳은 좌하단뿐이다 — 우상단은 Streamlit 기본 툴바(Deploy·⋮), 좌상단은 로고·회사명 헤더, 우하단은 위 버튼이다. 사이드바를 쓰지 않으므로(`navigation.py`) 좌하단은 비어 있다.

- 적용 후 로컬 실측(뷰포트 1280×800): 배지 `left=20 → right=170`, 하단 여백 20px, 배지 중심의 최상위 요소가 배지 자신(`stIFrame`), 내용 `다음 갱신까지 N초` 정상.
- **미검증**: 배포본에서 `Manage app` 버튼과 실제로 겹치지 않는지는 재배포 후 확인해야 한다. 로컬에는 그 버튼이 없어 재현할 수 없다.

**브라우저에서 센다.** 서버에서 매초 다시 그리면 그때마다 조회와 틱이 돌아 갱신 주기의 10배 비용이 든다. iframe을 띄우고 그 안의 스크립트가 센다 — `st.markdown(unsafe_allow_html=True)`는 `<script>`를 제거하므로 쓸 수 없다. fragment가 다시 실행되면 iframe이 새 시작 시각으로 다시 만들어져 카운트가 재시작한다. 0에 닿았는데 갱신이 아직 안 왔으면 "갱신 중…"으로 바꿔 멈춘 것처럼 보이지 않게 한다.

렌더 API는 **`st.iframe`** 이다 — 리포트 뷰어와 같다(§5-3, 2026-08-05 확정). `st.components.v1.html`은 2026-06-01자로 제거가 예고된 API라 쓰지 않는다.

#### 5-2-3. 값이 바뀐 지표 강조 (2026-08-11 사용자 요청)

카드가 갱신돼도 숫자가 조용히 바뀌기만 하면 **담당자는 무엇이 달라졌는지 알아채지 못한다.** 값이 바뀐 지표에만 1.2초짜리 강조를 건다(`CARD_CHANGE_FLASH_MS`).

**"바뀐 것"만 강조하는 것이 핵심이다.** 갱신 때마다 카드 전체가 번쩍이면 어느 것이 실제 변화인지 다시 알 수 없다. `components._changed_metrics()`가 직전 렌더의 값을 세션에 들고 비교해, 달라진 지표에만 `.changed` 클래스를 붙인다. 첫 렌더에는 비교 대상이 없으므로 아무것도 강조하지 않는다 — 바뀐 게 아니라 처음 보는 것이기 때문이다. `CARD_CHANGE_EPSILON`(0.05)보다 작은 변화도 무시한다: 표시가 소수점 1자리라 화면에서는 같은 숫자인데 강조만 들어가면 이유를 알 수 없는 깜빡임이 된다.

| 대상 | 효과 |
|---|---|
| 지표 값 | 브랜드색 배경이 번졌다 사라짐 (`metric-flash`, `CARD_CHANGE_FLASH_MS` 1.2초) |
| 게이지 채움 | 밝기 상승 후 복귀 + **width 전환**(`CARD_CHANGE_FLASH_MS`의 1/3 = 0.4초) — 값이 튀지 않고 움직여야 어느 방향으로 갔는지 눈으로 따라갈 수 있다 |
| 스파크라인 | **선이 왼쪽에서 오른쪽으로 그려진다** (`sparkline-draw`, `CARD_SPARKLINE_DRAW_MS` 0.7초). 가장 심각한 지표가 바뀐 카드만 |

강조색은 **브랜드 파랑**이다. 상태색(녹색·앰버·주황·빨강)은 설비 상태 전용이라 쓰지 않는다 — 강조가 상태 변화로 오독되면 안 된다.

##### 애니메이션 이름을 갱신마다 번갈아 바꿔야 한다 (2026-08-11 사용자 지적으로 발견)

처음 구현에서는 **강조가 처음 한 번만 재생됐다.** CSS 애니메이션은 요소가 새로 삽입되거나 애니메이션 속성이 바뀔 때만 재생되는데, Streamlit은 카드 마크다운을 갈아끼우지 않고 **DOM을 제자리에서 패치**하고(게이지 width 전환이 동작하는 것과 같은 이유), 연속으로 값이 바뀌는 지표는 `.changed`가 계속 붙어 있어 속성이 변하지 않았다.

실측으로 확인한 증상 — `animationstart` 누적:

| | 수정 전 `sparkline-draw` | 수정 후 |
|---|---|---|
| 갱신 1회 | 3건 | 2건 |
| 갱신 2회 | +1 | 4건 |
| 갱신 3회 | **+0** | 3건 |
| 갱신 4회 | **+0** | 4건 |

늘어난 소수는 "안 바뀌다가 바뀐" 요소뿐이었고, 계속 바뀌는 지표는 조용했다.

해결: 카드 루트에 `flash-a`/`flash-b`를 번갈아 붙이고(`config.CARD_FLASH_PHASES`, 값이 바뀔 때만 뒤집는다) 페이즈별로 같은 내용의 키프레임을 따로 둔다(`metric-flash-a`/`-b` 등, styles.py에서 생성). `animation-name`이 달라지므로 매번 다시 재생된다. **게이지 width 전환은 교대가 필요 없다** — 전환은 이름이 아니라 값이 바뀔 때 걸린다.

`prefers-reduced-motion: reduce`에서는 애니메이션과 전환을 모두 끈다(스파크라인은 `stroke-dasharray`도 함께 해제해야 선이 끊겨 보이지 않는다).

##### 왜 차트가 스스로 값을 보간하지 않는가 (2026-08-11 실측)

"요즘 차트 라이브러리는 데이터만 바꾸면 알아서 전환한다"는 말은 **같은 차트 인스턴스에 데이터를 갈아끼울 때** 성립한다(D3 data join, ECharts `setOption`, `Plotly.react`). 이 프로젝트에서는 두 가지 이유로 그 전제가 성립하지 않는다.

- **카드 스파크라인은 차트 라이브러리가 아니다.** `_sparkline_svg()`가 좌표를 이어 붙인 인라인 SVG `polyline`이다(카드 20장에 차트 라이브러리를 띄우면 렌더 비용이 카드 수만큼 붙어 일부러 이렇게 만들었다). `points` 속성은 CSS로 보간되지 않으므로 값 보간이 불가능하다. 그래서 **선 그리기**로 "새 데이터가 들어왔다"를 알린다 — `pathLength="1"`로 길이를 정규화해 두면 실제 경로 길이를 몰라도 `stroke-dashoffset` 1→0으로 그려진다(순수 CSS, 의존성 0).
- **모터 상세·그래프의 Altair(Vega-Lite) 차트는 데이터가 바뀔 때 통째로 교체된다.** 갱신 전 SVG 노드에 표시를 남기고 확인하니, 값이 바뀌는 10초 모터에서는 **생존 0개**(매번 교체)이고 값이 그대로인 300초 모터에서는 **생존 1개**(교체 안 함)였다 — Streamlit이 차트 spec이 같으면 갱신을 건너뛴다. 즉 라이브러리의 데이터 전환은 쓸 수 없지만(인스턴스가 유지되지 않으므로), **요소가 새로 만들어지는 시점이 곧 "값이 바뀐 시점"** 이라 CSS 애니메이션만으로 정확히 그때만 효과를 줄 수 있다.

  그래서 데이터 마크(선·점)에 `clip-path`로 **왼쪽부터 드러나는 효과**(`chart-reveal`, `DETAIL_CHART_REVEAL_MS` 0.7초)를 건다. 카드 스파크라인의 선 그리기와 같은 언어다. 선 그리기(`stroke-dashoffset`)가 아닌 이유는 Vega가 그린 `path`에 `pathLength`를 붙일 수 없어 경로 길이를 모르기 때문이고, `clip-path`는 길이에 무관하다. **축·눈금·임계선은 건드리지 않는다** — 축까지 쓸리면 차트가 통째로 새로 뜬 것처럼 보여 "값이 갱신됐다"는 뜻이 흐려진다.

  실측: 10초 모터 상세에서 갱신 4회 동안 매번 8건씩 재생(차트 4개 × 선·점 2그룹), 300초 모터에서는 4회 동안 **0건** — 변화 감지 코드 없이 저절로 게이팅된다.
- **유일하게 진짜로 보간되는 것은 게이지다.** CSS `div`라 `width` 전환이 걸린다 — 갱신 2회에 `transitionrun` 40건(=5장 × 4지표 × 2회) 발생을 확인했다. Streamlit이 카드 마크다운을 통째로 갈아끼우지 않고 DOM을 제자리에서 패치하기 때문에 가능하다.

실측: 첫 렌더 `.changed` 0개 → 갱신 후 지표 80개 중 20개(1행 5장 × 4지표, 곧 수집 주기가 짧은 모터들)만 강조. 값 플래시가 재생되고 2초 뒤 전부 잦아드는 것, 스파크라인이 `dashoffset` 0.51(중간) → 0으로 그려지는 것을 브라우저에서 확인했다.

**움직이는 카드는 첫 줄에 모았다 (2026-08-11 사용자 요청).** 종전에는 20장 중 3장만 갱신마다 바뀌었고 그 3장이 1행 5번째·4행 3번째·4행 4번째로 흩어져 있어, 시연에서 "실시간"을 가리킬 자리가 없었다.

카드는 심각도 순으로 배치되므로 **FAULT 모터가 첫 줄을 차지한다.** 그래서 시드가 FAULT를 부여하는 벌크 모터에만 짧은 수집 주기(`SEED_BULK_FAULT_INTERVAL_SECONDS`=10초)를 준다. 1행 5번째(커레이션 `2호기 메인 송풍기`, DANGER)는 원래 10초였으므로 **1행 5장이 모두 갱신마다 바뀐다**(실측 5/5).

나머지 15장은 300초라 5분마다 바뀐다. 벌크 전체를 짧게 하면 시드가 다시 무거워지므로(60초 시 2.02→5.73초) **기본 300초는 유지**한다 — `remaining_work` #2·#12.

**카드 안 지표 순서는 흔들리지 않는다.** `build_metric_readings()`는 심각도·비율 순으로 정렬하지만, `motor_card()`가 표시 직전 `METRIC_NAMES` 고정 순서로 다시 정렬한다. 갱신마다 노이즈로 비율 순위가 뒤집혀도(전류 0.440 vs 소음 0.428처럼 근접한 경우 실제로 뒤집힌다) 화면 순서는 그대로다. 스파크라인 대상(`readings[0]`)도 갱신 4회 동안 20장 전부 불변임을 실측했다.

### 5-5. 테마 (라이트 / 다크 / 시스템) — 2026-08-05 확정

**선택 UI는 Streamlit 기본 메뉴를 쓴다.** 우측 상단 `⋮ → Settings → Choose app theme`에 `Use system setting / Light / Dark` 세 가지가 이미 있다.

앱 안에 자체 선택 위젯을 두지 않은 이유는 다음 두 가지를 확인했기 때문이다.

- `st.context.theme`는 **읽기 전용**이고, 테마를 바꾸는 API가 없다 (`st.set_option`도 런타임 변경 불가).
- 따라서 자체 위젯으로는 Streamlit 자체 위젯(버튼·입력창·metric·dataframe·expander·알림 박스)의 색을 바꿀 수 없다. 전부 CSS로 덮어써야 하는데, 그 경우 사용자가 고른 테마와 Streamlit 위젯이 어긋날 위험이 크다.

**구성**

| 계층 | 담당 | 정의 위치 |
|---|---|---|
| Streamlit 자체 위젯 | Streamlit | `.streamlit/config.toml`의 `[theme.light]` / `[theme.dark]` |
| 앱 커스텀 CSS (카드·배지·게이지·헤더·로그인) | 앱 | `config.THEMES` 팔레트 → `app/ui/styles.py`가 주입 |

- 현재 테마는 `app/ui/theme.py`의 `current_theme()`이 `st.context.theme.type`으로 읽는다. 판별 불가 시 `DEFAULT_THEME`("light")로 떨어진다.
- 다크 팔레트는 색을 그대로 어둡게 만들지 않는다. **남색 브랜드색(`#1e3a8a`)은 어두운 배경에서 읽히지 않아 밝은 파랑(`#60a5fa`)으로 대체**하고, 상태색도 채도를 낮추고 밝기를 올려 대비를 확보한다.
- SVG(스파크라인)의 획 색은 CSS 변수를 쓸 수 없어 `palette()`에서 직접 가져온다.
- 리포트 템플릿(HTML/PDF)은 항상 흰 배경이므로 `STATUS_COLORS` 라이트 기준값을 그대로 쓴다 — 테마의 영향을 받지 않는다.

**헤더 안내 (§2-1)**: 앱에서 테마를 바꿀 수 없는 대신, 상단 헤더의 로그아웃 버튼 옆에 현재 테마와 변경 위치를 표시한다.

```
… (주)한국모터스        ☀️ 라이트        [로그아웃]
        김철수      ⋮ 설정에서 변경
```

아이콘·문구는 `config.THEME_LABELS` / `THEME_HINT`, 마우스를 올리면 전체 경로가 `THEME_HINT_TOOLTIP`으로 뜬다. 클릭 동작은 없다 — Streamlit 메뉴를 프로그램으로 열 수단이 없기 때문이다.

**검토했으나 채택하지 않은 방법**

- **Streamlit 위젯 CSS 전면 override + 자체 선택 위젯**: 버튼·입력·metric·dataframe·expander·알림·dialog 등 12종 이상을 직접 칠해야 한다. 얻는 것은 "선택 위치가 앱 안이냐 메뉴냐"의 차이뿐인데(테마 변경은 보통 1회성), 치르는 비용과 깨질 위험이 크다.
- **JS로 localStorage의 테마 값을 바꾸기**: `st.html(unsafe_allow_javascript=True)`로 가능은 하다. 그러나 적용하려면 페이지 리로드가 필요하고, **리로드하면 Streamlit 세션이 새로 만들어져 로그인이 풀린다**(§3.2에서 링크 방식이 실패한 것과 같은 원인). 테마를 바꿀 때마다 로그아웃되므로 쓸 수 없다.

**알려진 한계**: `st.context.theme`는 앱 첫 로드 직후나 테마 전환 직후에 이전 값을 반환할 수 있다(Streamlit 이슈 #11920). 이 경우 커스텀 CSS와 헤더 표기가 한 번의 리런만큼 늦게 따라온다.

### 5-3. 상태별 색상 팔레트 (2026-08-07 재정리)

**녹색 → 앰버 → 주황 → 빨강의 단조 증가 램프.** 심각도가 올라갈수록 색도 세진다.

| 상태 | 라이트 | 다크 | 배경(라이트/다크) |
|---|---|---|---|
| NORMAL | `#16a34a` 녹색 | `#4ade80` | `#f0fdf4` / `#0f2417` |
| WARNING | `#d97706` 앰버 | `#fbbf24` | `#fffbeb` / `#2a2010` |
| DANGER | `#ea580c` 주황 | `#fb923c` | `#fff7ed` / `#2a1a0e` |
| FAULT | `#dc2626` 빨강 | `#f87171` | `#fef2f2` / `#2b1414` |

**종전 FAULT는 슬레이트(`#1e293b` / 다크 `#cbd5e1`)였다.** "위험(적색)"과 "정지(회색)"를 구분하려는 의도였지만, **가장 심각한 상태가 화면에서 가장 약해 보이는** 역전이 생겼다. 다크에서는 `#cbd5e1`이 흰색처럼 보여 이상 지표 값이 정상 값보다도 흐렸다.

그 탓에 우회가 세 군데 쌓였고, 팔레트를 바로잡으면서 **전부 걷어냈다**.

| 걷어낸 우회 | 내용 |
|---|---|
| 조치 배너 | FAULT만 배경을 채워 강조 → 모든 배너가 같은 형태, 심각도는 색이 말한다 |
| 요약 타일 | FAULT 톤을 일부러 제외 → 고장이 있으면 FAULT색을 그대로 쓴다 |
| 이상 지표 값 | `METRIC_ALERT_COLORS`로 텍스트 색만 분리 → 상수 삭제, 상태색을 그대로 쓴다 |

**"노랑" 대신 앰버를 쓰는 이유 (실측).** 흰 배경에서 순수 노랑(`#eab308`)은 대비 **1.92:1**로 WCAG 텍스트 기준(4.5:1)에 한참 못 미친다. 읽히는 노랑(`#a16207`, 4.92:1)은 올리브·갈색으로 보여 노랑 구실을 못 한다. 앰버 `#d97706`을 유지하고 DANGER를 주황으로 밀었다.

**남은 접근성 한계.** 인접 단계끼리의 밝기 대비가 1.1~1.5:1이라 **색상(hue)으로만 구분된다.** 종전 FAULT 슬레이트는 밝기로도 구분됐지만(14.6:1) 램프에서는 그 성질이 사라진다. 적록색약 사용자를 위해 배지에는 상태 텍스트(`DANGER`, `FAULT`)를 항상 함께 표시한다 — 색만으로 판단하게 두지 않는다.

---

## 6. 관리자 페이지 (2026-08-11 추가)

기본 테이블 4종을 관리한다 — **고객 회사 / 담당자 / 모터 / 지표 임계값**. 텔레메트리·상태로그·알림 같은 시계열은 시드와 런타임 틱이 만들며 여기서 다루지 않는다. 상단 내비에 `관리자` 버튼을 넣되 **맨 뒤**에 둔다 — 매일 쓰는 감시 화면 셋과 성격이 달라 앞에 오면 동선을 흐린다.

구현: `app/pages/admin.py`(화면) + `app/services/admin.py`(검증·CRUD).

### 6.1 확정된 범위 (2026-08-11)

| 항목 | 결정 | 근거 |
|---|---|---|
| 접근 권한 | **로그인한 모두** | `company_contacts`에 role 컬럼이 없고 시연용 MVP라 도입하지 않는다 |
| 데이터 범위 | **자기 회사만** | 세션의 `company_id`로 모든 쿼리를 좁힌다 — 테넌트 경계는 지킨다 |
| 편집 범위 | **추가·수정·삭제 전부** | 삭제는 확인 창 + 연쇄 삭제로 처리 |
| 입력 데이터 유지 | **재시드 시 사라짐** | 아래 참조 |
| 엑셀 일괄 등록 | **버튼·안내만** (2026-08-12) | 고객에게 등록 방식을 소개하는 자리다. 파일 생성·파싱은 정식 서비스 — §6.3-A |

### 6.2 입력한 데이터는 재시드로 사라진다

부팅 시 최신 텔레메트리가 `DEMO_DATA_MAX_AGE_HOURS`(2시간)보다 오래되면 `bootstrap`이 **DB 파일을 통째로 지우고** 다시 만든다(`02 §6.1`). 그래서 관리자 페이지로 등록·수정한 내용도 함께 초기화된다. 배포본은 재시작할 때마다 파일시스템이 비므로 더 그렇다.

시연용 MVP라 이 동작을 그대로 두기로 확정했다(2026-08-11). 정식 서비스에서는 기본 테이블을 보존하도록 부트스트랩을 나눠야 한다.

> **화면 상단의 ⚠️ 재시드 경고를 없앴다 (2026-08-12 사용자 요청).** 종전에는 이 사실을 화면 맨 위 경고 박스가 직접 말했다(2026-08-11 결정). 그 박스가 화면 한 칸을 통째로 쓰면서 정작 관리할 표를 아래로 밀어냈다.
>
> 같은 요청으로 상단 캡션의 `로그인한 회사({company_id})의 정보만 보입니다`도 뺐다 — 헤더가 이미 회사명을 보여주고 있어 중복이었다. 그전(2026-08-12)에는 이 경고에서 내부 문서 경로 `` (`02_architecture.md` §6.1) ``를 먼저 뺐는데(고객 의견 MVP 1-(3)), 경고 자체가 사라지면서 그 처리도 함께 없어졌다.
>
> **남은 리스크**: 관리자 화면에서 등록·수정한 내용이 재시드로 사라지는 것을 이제 화면이 미리 알리지 않는다. 시연 중에는 문제가 되지 않지만(재시드 조건은 앱을 2시간 이상 껐다 여는 것), 데이터를 넣어 두고 다음 날 없어지면 버그로 보일 수 있다. 이 문서와 README에만 남는다.

**새로 등록한 모터에는 계측값이 없다.** 시드는 부팅 때 한 번만 돌고 런타임 틱은 마지막 행이 있는 모터만 이어 붙이므로(`services/runtime_tick.py`), 대시보드 카드에 "수집된 계측값이 없습니다"로 나오는 것이 정상이다 — 이 또한 화면이 미리 알린다.

### 6.3 탭별 동작

- **고객 회사** — **조회 전용이다 (2026-08-12 사용자 요청).** 회사 ID와 회사명을 `disabled=True`로 보여주기만 하고, 폼과 저장 버튼을 없앴다. **화면 캡션은 한 줄이다** — `회사 정보는 조회만 합니다 — 회사명 변경은 계약 처리로 진행합니다.` 종전에는 추가·삭제를 두지 않는 이유까지 3줄로 적어, 비활성 입력 2칸짜리 탭에서 설명이 내용보다 길었다(2026-08-12 고객 의견 2차 2-(5)). 그 이유는 아래 문장과 소스 주석에만 둔다. 종전에는 회사명만 수정할 수 있었는데 회사명이 바뀌는 경우는 사명 변경뿐이고 그건 계약 처리라 고객 화면에서 할 일이 아니다 — 누를 일이 없는 버튼이 탭 하나를 채우고 있었다. 추가·삭제는 원래부터 두지 않는다: 자기 회사만 보이는 구조라 추가해도 화면에 나타나지 않고, 자기 회사를 지우면 로그인이 성립하지 않는다.
  - `services/admin.py`의 `update_company()`는 **호출부가 0이 됐지만 남겨 뒀다** — 되살릴 때 검증 규칙(빈 회사명 거부)을 다시 쓰기 위해서다. docstring에 미사용임을 적었다.
- **담당자** — 목록 + 등록/수정/삭제. 이메일이 로그인 ID이며 `UNIQUE`라 중복은 DB 제약으로 잡아 메시지로 돌려준다. 수정 시 비밀번호를 비워 두면 기존 값을 유지한다(bcrypt 해시로 저장).
- **모터** — 목록 + 등록/수정/삭제. `motor_id`는 등록 후 변경 불가다(계측 데이터 수만 행이 참조한다). 수집 주기는 `ALLOWED_COLLECTION_INTERVALS_SECONDS`(10/20/30초) 중에서만 고른다(`02 §2.1`).
  - **수명·구동일자 입력 필드는 아직 없다 (2026-08-13).** `motors` 테이블에 `lifespan_hours`(설계 수명, 시간)와 `operation_started_at`(실제 가동 시작일) 컬럼은 추가됐지만(`04 §3.3`), 이 폼에는 두 필드가 없어 **관리자가 지금 모터를 등록하면 두 값이 비어 들어간다.** 시연 데이터 210대는 시드가 채워 둔 상태다. 수명 관리 기능 전체(가동시간 산출 규칙, 경과율 계산, 화면 노출, 교체 시기 알림, 이 입력 필드)는 `remaining_work` #22에 남겼다.
- **지표 임계값** — 모터를 고르고 `st.data_editor`로 4행을 편집한다.
  - **표시 순서는 판정 규칙 → 안내 박스 → 모터 선택 → 표다 (2026-08-12 사용자 요청).** 규칙과 안내는 어느 모터를 고르든 같은 이야기라 위에 모으고, 모터 선택은 바로 아래 표와 붙어 있어야 "이 모터의 값을 고친다"가 한 덩어리로 읽힌다.
  - **셀렉트박스 항목이 수집 주기를 함께 말한다** — `MTR-001 · 2호기 메인 송풍기 (수집 주기 10초)`. 바꾼 기준이 언제부터 적용되는지 판단하려면 그 모터의 주기를 알아야 하는데, 모터를 고르는 자리에서 바로 보이는 편이 낫다. 그래서 안내 박스에서 `이 모터의 수집 주기는 N초입니다` 문장을 뺐다 — 같은 값을 두 곳에 적을 이유가 없다.
  - 이 덕분에 **안내 박스가 모터에 의존하지 않게 되어** 셀렉트박스보다 먼저 그릴 수 있다. (모터별 값이 남아 있었다면 위젯이 값을 정한 뒤에야 문구가 완성되므로 `st.container()`로 출력 자리를 미리 잡아야 했다.) **정상 < 경고 < 위험 < 고장** 순서를 저장 전에 검사한다 — 순서가 뒤집히면 상태 판정이 성립하지 않아 화면이 조용히 잘못된 상태를 보여준다.
  - **바꾼 기준은 다음 수집분부터 적용된다.** 이미 저장된 판정과 발행된 리포트는 그대로 둔다(`04 §2.2`). 화면이 그 사실과 해당 모터의 수집 주기를 함께 안내한다 — 바꿨는데 카드 색이 안 변하면 "설정이 안 먹었다"고 오해하기 때문이다. 반면 게이지 눈금·차트 임계선처럼 "지금 기준"을 보여주는 표시는 즉시 바뀐다.
  - **이벤트·알림은 생기지 않는다는 사실을 함께 안내한다 (2026-08-12 추가).** 임계를 낮추면 다음 수집분부터 새 기준으로 판정되어 **카드 색과 요약 타일은 바뀌지만**, 이벤트 목록·리포트·알림에는 아무것도 생기지 않는다. 런타임 틱이 텔레메트리만 이어 붙이고 `motor_status_logs`에 쓰지 않는데(`services/runtime_tick.py run_tick()`), 이벤트 목록과 리포트는 그 테이블에서 나오기 때문이다(`services/events.py`). 상태 전이 감지는 MVP 범위 밖이다(`remaining_work` #2-1). 시연에서 임계값을 낮춰 보이는 순간 "그럼 알림은 갑니까"가 반드시 따라오므로 화면이 먼저 말한다(고객 의견 MVP 1-(2)).
  - **이 두 안내는 한 박스다 (2026-08-12 사용자 요청).** 처음에는 "언제부터 적용되는가"(`st.info` 🕒)와 "그래도 알림은 안 간다"(`st.warning` 🔔)를 색이 다른 박스 둘로 나눴는데, 표를 편집하기 전에 읽어야 할 것이 화면 두 칸을 차지했다. 같은 질문("바꾸면 무슨 일이 일어나는가")에 대한 답이라 `st.info`(🕒) 하나에 두 문단으로 합쳤다.
  - **변경 이력을 함께 보여준다** — 언제 누가 어떤 값을 바꿨는지 최근 `ADMIN_THRESHOLD_HISTORY_LIMIT`(20)건(`motor_threshold_history`, `04 §3.9`). 값이 실제로 달라진 지표만 남는다.

### 6.3-A 엑셀 일괄 등록 — 버튼과 안내만 둔다 (2026-08-12 사용자 요청)

**모터 탭과 지표 임계값 탭에 `엑셀 샘플 다운로드` · `업로드 등록` 버튼 두 개를 둔다.** 모터 200대를 화면 폼으로 한 대씩 넣는 것은 실제 도입에서 성립하지 않으므로, 고객에게 "이렇게 등록하게 됩니다"를 보여주는 자리다. **MVP는 파일을 만들지도 읽지도 않는다** — 파일 생성·파싱은 정식 서비스에서 붙인다.

| 위치 | 이유 |
|---|---|
| 모터 탭: `등록된 모터 N대` 캡션 아래, 표 위 | 아래 편집 폼은 한 건짜리이고 이건 표를 통째로 채우는 방법이라, "무엇으로 이 표를 채우나"가 표보다 먼저 나와야 순서가 맞는다 |
| 지표 임계값 탭: 판정 규칙·적용 시점 안내 아래, 모터 선택 위 | 같은 이유. 다만 "바꾸면 무슨 일이 일어나는가"(§6.3)를 먼저 읽어야 하므로 그 뒤에 둔다 |

- **비활성 버튼으로 두지 않는다.** 눌러도 아무 반응이 없으면 기능 소개가 아니라 고장난 화면으로 읽힌다. 누르면 무엇을 할 수 있는지 `st.info`로 안내가 뜬다 — `st.toast`는 몇 초 뒤 사라져 설명하는 동안 화면에 남지 않는다.
- **안내는 "무엇을 준비해야 하나"에 답한다.** 샘플 쪽은 양식의 열 구성을, 업로드 쪽은 올린 뒤 무슨 검사가 도는지를 말한다.

| 대상 | 샘플 안내(열 구성) | 업로드 안내(검사) |
|---|---|---|
| 모터 | 모터 ID · 모터명 · 설치 위치 · 모델명 · 시리얼 번호 · 수집 주기(초) | 형식·중복 ID 검사 후 일괄 등록, 문제 행은 행 번호와 사유를 함께 |
| 지표 임계값 | 모터 ID · 지표(온도/진동/전류/소음) · 정상 · 경고 · 위험 · 고장 | `정상 < 경고 < 위험 < 고장` 검사 후 일괄 반영, 다음 수집분부터 적용 |

- 공통 꼬리 문구는 `정식 서비스에서 제공됩니다. 지금은 아래 양식으로 한 건씩 등록합니다.`다. **화면에 "시연"이라는 단어를 두지 않는다** — §6.3의 안내 박스와 같은 기준이다(캡처 한 장이 돌면 그 화면은 계속 "시연용"으로 읽힌다).
- 문구·버튼 라벨·key 접두사는 전부 `config.ADMIN_BULK_*`에 모았다. 렌더는 `pages/admin.py`의 `_bulk_actions(target)` 하나로 두 탭이 공유한다.

### 6.4 삭제는 연쇄 삭제다

`PRAGMA foreign_keys = ON`(`db/connection.py`)이라 참조가 남아 있으면 삭제가 실패한다.

- **모터**: 확인 창이 함께 지워질 건수(텔레메트리 행 / 상태 전이 로그 / 알림 이력 / 임계값 4건)를 먼저 보여주고, 동의 체크 전에는 [삭제]가 비활성이다. 딸린 텔레메트리가 수백~수만 행일 수 있어 숫자를 보지 않고 지우게 두면 안 된다.
- **담당자**: `notification_logs.contact_id`가 **NOT NULL**이라 알림 행을 지워야 담당자를 지울 수 있다. 정비 확인·로그인 이력은 NULL 허용이라 참조만 끊고 기록은 남긴다. **로그인 중인 본인**과 **회사의 마지막 담당자**는 삭제를 막는다 — 각각 세션이 유령 담당자를 가리키게 되고, 그 회사로 로그인할 방법이 사라지기 때문이다.

### 6.5 셀렉트박스는 드롭다운 선택만 허용한다 (2026-08-11 사용자 요청)

편집 대상·모터·수집 주기 셀렉트박스에 타이핑을 막는다 — 목록에 없는 값을 입력할 이유가 없다. 모터 그래프의 표시 범위 필터와 **같은 컴포넌트**(`components.lock_selectbox_typing()`)를 쓴다. 종전에는 그래프 페이지에만 인라인 스크립트로 있던 것을 컴포넌트로 빼 두 화면이 공유한다.

CSS로는 막을 수 없다(드롭다운을 열면 입력이 자동 포커스된다). 부모 DOM에 접근하는 `st.iframe` 스크립트로 `readOnly`·`inputmode="none"`을 걸고, rerun 후 재렌더된 입력에도 다시 걸리도록 `MutationObserver`를 둔다. **관찰자는 매번 새로 설치한다** — 페이지를 옮기면 이전 페이지의 iframe이 사라지면서 그 안에서 만든 콜백도 죽는데, 죽은 콜백을 붙들고 있으면 새 페이지에서 재적용이 되지 않는다.

**폭도 제한한다.** 값이 짧은데 입력칸만 1,400px가 되면 어디를 눌러야 하는지 오히려 흐려진다. `st.columns`의 첫 칸에만 그린다 — 편집 대상은 2/4(모터명이 들어가야 한다), 수집 주기는 1/4(두 자리 숫자). 실측 712px / 344px.

### 6.6 자동 갱신을 넣지 않는다

관리 화면에는 `st.fragment(run_every=...)`를 쓰지 않는다. 입력 중인 폼이 10초마다 다시 그려지면 타이핑하던 값이 날아간다. 사용자가 저장을 누를 때만 바뀌면 된다.

### 6.7 검증 (2026-08-11)

DB 사본에 대고 서비스 계층을, 브라우저로 화면 흐름을 확인했다.

| 확인 | 결과 |
|---|---|
| 모터 등록 | 모터 1행 + 임계값 4행 생성 |
| 중복 모터 ID / 시리얼 | 차단, 메시지 표시 |
| 수집 주기 45초 | 차단 (10/20/30만 허용) |
| 임계값 역순 저장 | 차단 |
| 중복 이메일 | 차단 |
| 본인 삭제 | 차단 |
| 모터 연쇄 삭제 | 텔레메트리 291행·상태로그 1건·임계값 4건까지 잔존 0 |
| 화면 전체 흐름 | 등록(200→201대) → 확인 창(건수 표시) → 삭제(201→200대) |
| 셀렉트박스 잠금 | 4개 전부 `readOnly=true`, "zzz" 타이핑해도 값이 `＋ 새로 추가` 그대로 |
| 셀렉트박스 폭 | 편집 대상 712px(2/4) · 수집 주기 344px(1/4) — 컨테이너 1440px 기준 |

---
승인해주시면 다음 문서(`06_report_spec.md`) 작성을 진행하겠습니다.
