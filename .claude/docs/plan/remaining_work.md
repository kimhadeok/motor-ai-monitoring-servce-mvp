# 남은 작업 (Remaining Work) — 추적 문서

> 목적: MVP 완성을 위해 남은 작업을 한눈에 관리한다. 작업을 진행하며 완료 항목을 체크(`[x]`)로
> 갱신한다.
>
> **진행 규칙**
> - 항목을 시작할 때 `상태`를 `진행 중`으로, 끝나면 체크박스 `[x]` + `상태: 완료(커밋)`으로 바꾼다.
> - 구현 중 내린 결정·추가 의견은 이 문서뿐 아니라 관련 **`.claude/docs/generated/` 스펙 문서(01~06)** 에도 반영한다(단일 소스 오브 트루스 유지).
> - 검증은 다크 모드로 수행한다.
>
> 최종 갱신: 2026-08-10 · 기준 커밋: `motor-038`

---

## 현재까지 완료 (참고)
데이터 모델·런타임 시드(COMP-001 200대)·로그인/인증·메인 대시보드(요약·배너·정비확인·카드·이벤트)·
모터 상세·**모터 그래프**·**모터 현황**·리포트 HTML 폴백은 구현·검증 완료.
2026-08-07 추가: **참조 자료 RAG 인제스트 + 지식 계층 분리 + 부팅 경량화**(아래 9번).
2026-08-10 추가: **LangGraph 진단 에이전트**(아래 1번) — 리포트의 "AI 진단"이 실제 LLM 생성물이 됐다. 남은 것은 아래.

---

## 🔴 핵심 기능 (미구현)

- [x] **1. LangGraph 진단 에이전트** (2026-08-10 완료)
  - 확정(2026-08-06): (A) 최소 LangGraph 그래프 (B) 온디맨드 생성(부팅 대량 생성 제거) (C) 섹션 = 요약·원인·연쇄영향·방치결과. **(B)는 2026-08-07 선행 완료**(실측 8.80초 절감), 이번에 (A)·(C) 구현.
  - 신규 `app/agents/schema.py`(`DiagnosisPayload`/`DiagnosisResult`/`DiagnosisContext`), `app/agents/diagnosis_agent.py`(`prepare`→`diagnose`→`finalize`), `app/prompts.py` 채움.
  - **수치 인용 검증 게이트 추가.** 근거를 프롬프트에 넣어줘도 GPT-4o가 "온도가 급격히 상승하여 냉각 문제로 보입니다" 같은 일반론을 반환하는 것을 실측했다. `finalize`가 요약·원인의 숫자를 실제 측정값과 대조해(오차 0.05) 하나도 안 맞으면 규칙 기반으로 교체한다.
  - **폴백 시 진단 모델 라벨 전환**(2026-08-10 확정): `"규칙 기반 진단 (LLM 미사용)"`. 호출하지 않은 모델명을 적으면 담당자가 LLM 생성물로 오독한다.
  - **진단 결과는 DB에 따로 저장하지 않는다**(2026-08-10 확정) — 기존 `report_html` 캐시만 사용, 스키마 변경 없음. 별도 진단 화면을 만들 때 컬럼을 추가한다.
  - **지연 import 유지.** `langgraph` 3.17초 + `langchain_openai` 0.96초 콜드 import를 부팅에 올리지 않는다. 앱 진입 경로 import(1.33초) 후 두 모듈이 `sys.modules`에 없음을 실측 확인.
  - 실측: 첫 열람 5~9초(3회 5.29 / 6.03 / 8.53) → 2건째 4.12초 → 재열람 캐시 0.03초 / 폴백 2.52초(키 있음·LLM 오프), 0.37초(키 없음).
  - **남은 미검증**: Streamlit 다이얼로그 안의 실제 화면(Browser pane 미표시로 스크린샷 불가 — 리포트 HTML은 DOM·레이아웃까지 실측했으나 앱 안에서의 모습은 미확인). PDF 출력은 5번에 포함.
  - 계획: `.claude/docs/plan/2026-08-06_langgraph-diagnosis-agent.md`, `.claude/docs/plan/2026-08-10_langgraph-diagnosis-agent-impl.md`
  - 반영 스펙: `01_tech_stack.md §2.4`, `02_architecture.md §2.4.1/§2.5/§6.1.1/§6.4`, `04 §3.5`, `05 §3.3`, `06_report_spec.md §2.3/§3/§3.1`
  - 상태: **완료 (`motor-039`)**

- [ ] **2. 실시간 런타임**
  - [ ] 2-1. 상태 전이 **실시간 감지** — 현재는 시드 시점에만 전이 생성. 수집→판정→전이 로직 필요. (스펙: `03_state_event_logic.md`, `02_architecture.md`)
  - [ ] 2-2. **자동 갱신** — `st.fragment(run_every=...)` 미적용(`DASHBOARD_REFRESH_INTERVAL_SECONDS` 상수만). (스펙: `05_ui_screens.md §5-2`)
  - [ ] 2-3. **통신 두절 판정**(connectivity/NO_DATA) — 스키마·`MISSED_CYCLES_THRESHOLD` 상수만, 런타임 판정 없음. (스펙: `03_state_event_logic.md §4.4`)
  - 상태: 미착수

- [ ] **3. 알림 실제 발송**
  - 현황: `notification_logs` 적재만, KAKAO/SMS/EMAIL 실제 전송 없음.
  - 할 일: 발송 어댑터(모의/실 채널) + 쿨다운 연계. (스펙: `04_database_schema.md §3.7`, `02_architecture.md`)
  - 상태: 미착수

- [ ] **4. 48시간 보관 배치**
  - 현황: `apscheduler` 의존성·`RETENTION_BATCH_CRON_HOUR` 상수만, 배치 코드 없음.
  - 할 일: 보관창(48h) 초과 텔레메트리 정리 배치. (스펙: `04_database_schema.md §5-5`)
  - 상태: 미착수

## 🟡 배포·검증 (미검증)

- [ ] **5. 배포 검증**
  - Python 3.14 + Streamlit Community Cloud 실배포, PDF(WeasyPrint) 배포 환경 동작 확인. `packages.txt` 점검(Cloud는 **Debian 11 bullseye** 기준으로 apt 설치 — 현재 목록의 `libgdk-pixbuf2.0-0`은 bullseye 이름이라 맞다).
  - **배포 설정 확정(2026-08-10)**: Secrets에 `DIAGNOSIS_LLM_ENABLED="false"`. 공개 URL + 로그인 화면의 시연 계정 노출로 GPT-4o 호출을 통제할 수단이 없다. **`OPENAI_API_KEY`는 유지** — 비우면 RAG까지 죽어 `rag_ready=False`가 된다(실측). AI 진단 시연은 로컬에서 스위치를 켜고 한다.
  - **사전 확인 완료(2026-08-10)**: `requirements.txt`는 `uv.lock`과 동기화됨(langgraph 1.2.10 포함). 메모리 실측 228MB — Cloud 한도 690MB~2.7GB 대비 여유. 타임존은 `DISPLAY_TIMEZONE="Asia/Seoul"` 고정이라 Cloud UTC 무관.
  - **판정 방법 (2026-08-10 마련)**: 배포 후 Manage app 로그의 부팅 두 줄로 대부분을 판정한다 (`02_architecture.md §6.5`).
    `환경 | python=… chromadb=… | openai_key=… diagnosis_llm=…` → Python 버전·chromadb 버전·Secrets 반영 여부.
    `부트스트랩 완료 | … rag_ready=… rag_chunks=… | N초` → RAG 적재 여부와 부팅 시드 소요.
    RAG 미적재 시 WARNING이 함께 뜬다. 리포트를 열면 `진단 생성 | … source=llm|rule` 이 남아 LLM 동작 여부도 확인된다.
  - **추가 확인 항목(2026-08-07)**: 커밋한 `data/chroma/`(1.3MB)가 배포본에서 정상 로드되는지. 부팅 summary의 `rag_ready`가 `True`이고 `rag_chunks=44`(2026-08-10 로컬 실측치)면 성공, `False`면 SOP가 키워드 폴백으로 동작 중. 종전 이 항목에 적혀 있던 46은 실제 적재량과 다른 값이었다. chromadb 버전이 빌드 환경과 달라지면 persist 포맷 호환이 깨질 수 있으므로 `pyproject.toml` 핀 확인 필요. **미검증**.
  - 상태: 미검증

## 🟢 정리 (문서·성능)

- [x] **6. README 최신화**
  - `README.md` "현재 범위"·"프로젝트 구조"를 실제 구현 상태로 정정(대시보드 요약·모터 카드·상세는 구현됨으로, 모터 그래프/현황 추가, pages/ui 구조 반영). remaining_work 링크 추가.
  - 상태: 완료(커밋 예정)

- [ ] **7. 메인 대시보드 성능**
  - COMP-001 200대 → 무거운 모터 카드 200개 렌더. 전체 목록은 '모터 현황'이 담당하므로 대시보드 카드 그리드 축소(조치 필요/상위 N)를 검토.
  - **부분 조치(2026-08-07)**: `config.DASHBOARD_MOTOR_CARD_LIMIT`(현재 20)로 **렌더 수를 제한**했다. 심각도 순으로 골라(`select_priority_cards()`) 위험 모터가 가려지지 않게 한다. 개수는 임시값 — 추후 확정 필요. 반영: `05_ui_screens.md §3.2`.
  - **남은 것**: 쿼리 비용은 그대로다. `list_company_motors()`가 200대 전부에 대해 모터당 `last_changed`·`telemetry`·`trend`·`fault_metrics` 쿼리를 돌리므로, 표시할 만큼만 조회하도록 줄이는 작업이 필요하다.
  - 상태: **부분 완료** (렌더 제한 적용, 쿼리 최적화 미착수)

- [x] **8. (사소) 전류 그래프 중간 눈금 표기**
  - 해결: Y축에 `format=".1~f"` 적용 → 12.5가 "13"이 아니라 "12.5"로 표기(불필요한 0은 제거). 다크에서 `0 · 12.5 · 25` 확인.
  - 상태: 완료(커밋 예정)

- [x] **9. 참조 자료 RAG 인제스트 + 부팅 경량화** (2026-08-07)
  - `uploads/Reference/` PDF 3종(117p) 분석 후 지식 3계층 분리: 관계형(`data/knowledge/fault_modes.json` — 고장모드 9종·지표 매핑 17건) / 서술형(ChromaDB 44청크) / 제외(Ford 튜토리얼 본문, 영업·회사 정보).
  - **그래프DB 미도입 결정** — 그래프성 내용은 실재하나 총 26행·최대 2홉이라 커밋된 JSON 직접 조회로 충분. 근거: `01_tech_stack.md §2.3.1`.
  - **부팅에서 RAG 인제스트·리포트 전건 생성 제거** → 콜드 스타트 약 14.6초 → **4.43초 실측**. 부팅 경로에 OpenAI API 호출 0회.
  - 리포트에 "의심 고장 모드" 섹션 추가(근거·부품·출처·여유 시간대). SOP 질의문을 고장모드 어휘로 보강 + `doc_type` 필터.
  - `scripts/build_knowledge.py` 신규(수동 1회 + `--dry-run`), `data/chroma/`(1.3MB) 커밋 전환.
  - 계획: `.claude/docs/plan/2026-08-07_reference-pdf-ingestion.md`
  - 반영 스펙: `01_tech_stack.md §2.3.1`, `02_architecture.md §2.2/§6.1/§6.1.1/§6.4/§6.5`, `06_report_spec.md §2.3/§2.4/§3`
  - 상태: 완료(커밋 예정) — 단, **배포 환경 동작은 5번에서 미검증**

- [x] **10. 화면·리포트 정리 (2026-08-07)** — 시연 품질 개선. 항목별 근거는 반영 스펙 참조.
  - **상태 팔레트 재정리**: 녹색 → 앰버 → 주황 → **빨강(FAULT)** 단조 증가 램프. 종전 FAULT 슬레이트는 가장 심각한 상태가 가장 약해 보여, 배너 채움·요약 타일 톤 제외·지표 텍스트 색 분리라는 **우회 3건**을 만들고 있었다 — 팔레트를 고치면서 전부 제거. 반영: `05 §5-3`.
  - **대시보드 상단 요약**: `st.metric` → 자체 카드(`summary_tiles`). 액센트 색이 곧 상태. 반영: `05 §3.1`.
  - **모터 카드 표시 수 제한**: 심각도 순 20대(`DASHBOARD_MOTOR_CARD_LIMIT`). 반영: `05 §3.2`, 7번.
  - **모터 현황 카드**: 한 줄 10개(128px) → **7개(170px)**. 10개에서는 지표 줄 400개가 전부 잘렸다. 반영: `05 §3-B`.
  - **이벤트 목록**: **값 변화 열** 추가(`94.73 → 95.12 dB`, 소수 2자리·동률 시 자릿수 증가), 행/헤더 구분선, 컬럼명 16px, 열 폭 실측 재배분, **FAULT·DANGER 전이만 표시**(`DASHBOARD_EVENT_STATUSES`). 반영: `05 §3.3/§4.4`, `04 §3.5`.
  - **리포트**: 지표별 임계값 표 추가, **섹션당 한 페이지(5페이지)** 로 재구성, 섹션 번호 독립 부여(2 AI 진단 / 3 근본 원인 분석 / 4 SOP / 5 대응 이력), 모든 내용 페이지에 머리글 반복. 반영: `06 §3.1`.
  - **데모 데이터 신선도 판정**: 모터 존재만 보던 판정이 낡은 DB를 통과시켜, 시드 6.6시간 뒤 모터 그래프가 전부 "데이터 없음"이 됐다. 최신 텔레메트리 나이를 함께 보고 `DEMO_DATA_MAX_AGE_HOURS`(2h) 초과 시 재생성. 반영: `02 §6.1`, `README`.
  - 상태: 완료(커밋 예정) — PDF 실제 출력은 5번에서 **미검증**(Windows에 WeasyPrint 네이티브 라이브러리 없음).

---

## 진행 로그
- 2026-08-06: 문서 생성 (`motor-026` 기준).
- 2026-08-06: 6번(README 최신화)·8번(전류 눈금 표기) 완료. 다크 모드 검증.
- 2026-08-07: 9번(참조 자료 RAG 인제스트 + 부팅 경량화) 완료. 1번의 (B) 온디맨드 생성이 여기서 선행 완료됨. (`motor-035`)
- 2026-08-07: 10번(화면·리포트 정리) 완료 — 팔레트·요약 타일·카드 수·이벤트 목록·리포트 5페이지·데모 신선도.
- 2026-08-10: 1번(LangGraph 진단 에이전트) 완료 — 최소 그래프·구조화 4섹션·수치 인용 검증·규칙 기반 폴백·폴백 라벨 전환. 남은 핵심 기능은 2·3·4번.
