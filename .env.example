# LLM / RAG — 앱이 읽는 환경변수는 아래 둘뿐이다 (app/config.py).
# 나머지 설정값은 전부 app/config.py에 중앙화되어 있다.
OPENAI_API_KEY=

# 진단 에이전트 강제 오프 스위치 (기본 true). false로 두면 LLM을 호출하지 않고
# 규칙 기반 진단으로 리포트를 만든다 — 오프라인 시연·폴백 확인용.
# 키가 없을 때도 자동으로 같은 경로를 탄다.
DIAGNOSIS_LLM_ENABLED=true
