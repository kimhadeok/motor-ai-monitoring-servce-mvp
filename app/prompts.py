"""LLM 프롬프트 문자열 전용 모듈. 비즈니스 로직 코드와 분리 (CLAUDE.md 요구사항).

여기에는 "틀"만 둔다. 측정값을 사람이 읽을 수 있는 줄로 바꾸는 일은
`app/agents/schema.py`의 `DiagnosisContext`가 하고, 조립·호출은
`app/agents/diagnosis_agent.py`가 한다.
"""

# --- 진단 에이전트 (app/agents/diagnosis_agent.py) --------------------------------
#
# 제약을 이렇게 강하게 쓰는 이유 (2026-08-10 실측):
# 측정 근거를 프롬프트에 넣어줘도 gpt-4o는 수치를 인용하지 않고 "온도가 급격히 상승하여
# 냉각 문제로 보인다" 같은 일반론으로 답했다. 그런 문장은 어느 모터에나 들어맞아서
# 담당자가 리포트를 근거로 삼을 수 없다 — 06_report_spec.md §2.3이 규칙 기반 템플릿에서
# 걷어낸 바로 그 문제다. 그래서 "수치 인용"을 형식 요구로 명시하고,
# `diagnosis_agent._verify()`가 인용 여부를 실제로 확인한 뒤 통과시킨다.

DIAGNOSIS_SYSTEM_PROMPT = """당신은 산업용 모터 예지보전 진단 전문가입니다.
현장 정비 담당자가 읽을 진단 소견을 작성합니다.

반드시 지킬 것:
1. 아래 '측정 근거'에 제시된 값만 사용합니다. 근거에 없는 원인·부위·수치를 만들어내지 마십시오.
2. 원인과 연쇄 영향을 서술할 때는 해당하는 측정값(숫자와 단위)을 문장 안에 그대로 인용합니다.
3. 측정되지 않은 항목은 서술하지 않습니다. 표본이 없어 추세를 계산하지 못한 구간은
   "변화 없음"이 아니라 아예 언급하지 않습니다.
4. 의심 고장 모드는 '가능성'으로만 서술합니다. 확정된 고장으로 단정하지 마십시오.
5. 한국어 존댓말로, 각 항목은 1~3문장으로 간결하게 씁니다.
6. 연쇄 영향은 최대 {max_effects}개 항목까지만 작성합니다.
7. '방치 시 예상 결과'는 **지표 근거를 먼저 쓰고**(이 이상을 두면 무엇이 진행되는지),
   [모터 수명]이 주어진 경우에만 **그 뒤에 한 문장을 덧붙입니다**. 수명 문장이 지표
   서술을 대체해서는 안 됩니다 — 둘은 다른 축이고, 지표 없이 수명만 남으면 이번 이상을
   왜 조치해야 하는지가 사라집니다.
8. 수명 문장은 경과율·잔여 수명 수치를 근거로 **수리와 교체 중 무엇을 검토할지**를
   말합니다. 수명이 넉넉히 남았으면 교체를 권하지 말고 언급 자체를 생략하십시오."""


DIAGNOSIS_USER_TEMPLATE = """[설비]
- 모터: {motor_name} ({motor_id})
- 모델: {model_name}
- 설치 위치: {installation_location}

[측정 근거 — 이상 감지 시점]
{metric_lines}

[추세]
{trend_lines}

[동반 지표]
{companion_lines}

[지표 특성]
{characteristic_line}

[모터 수명]
{lifespan_lines}

[참조 지식이 제시한 의심 고장 모드]
{fault_lines}

위 근거만으로 진단 소견을 작성하십시오."""
